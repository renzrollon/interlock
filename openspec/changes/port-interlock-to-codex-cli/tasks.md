## 1. The canonical model class

- [ ] 1.1 Create `lib/models.mjs` and `test/spine/models.test.mjs`. **First**, verify the pinned Codex model ids (`gpt-5.6-terra`, `gpt-5.6-luna`) against the live Codex models documentation and correct design.md §Decisions 2 and 8 if either differs — every table below reads from this module, so a wrong id here propagates everywhere. Then define: the closed class set `cheap | standard | deep`; `canonicalModelClass(raw)` returning the canonical class or `null` (trim, lowercase, reject anything else — this is the single normalization boundary per design.md §Decision 1); `clampClass(class, tier)` with `deep`→`standard` below tier 5 and `cheap` preserved; `resolveModel(hostId, class, tier)` returning `{ model, effort }` — Anthropic slugs with no effort for the Claude host, the tier→(model, effort) table from design.md §Decision 2 for the Codex host, honoring `INTERLOCK_CODEX_MODEL_{CHEAP,STANDARD,DEEP}` overrides; and `formatModelRouting()` rendering the published table. Resolution MUST be total — every class on every host — and an unresolvable pair MUST throw rather than return a default. Pin the class set, the clamp truth table, the full tier ladder, the override behavior, and totality across all hosts in the test.

## 2. Sweep every reader onto the class

Every task in this section imports from `lib/models.mjs`; none needs another's output. No artifact may hold a vendor model name in the `model` position after this section — plan validation is the enforcement point, so a missed reader fails at the boundary rather than silently downstream.

- [ ] 2.1 `lib/waves.mjs` + `test/spine/waves.test.mjs`: replace the `MODELS` set with the imported class set; route plan validation through `canonicalModelClass` and fail naming the task id and the permitted classes; rewrite `clampModel` to call `clampClass` preserving the existing recorded shape (`{ id, from, to, tier }`); make `laneModel` return the hardest task's class defaulting to `standard`; write the class in the plan build; print classes rather than slugs in the plan preview and the agent bill. Cover the two-inputs-differing-only-in-casing case that `model-routing`'s edge-case scenario requires.
- [ ] 2.2 `bin/interlock` + `test/spine/cli.test.mjs`: `laneModelOf` and the `wave-state next` payload carry the class.
- [ ] 2.3 `lib/run-log.mjs` + `test/spine/run-log.test.mjs`: record **both** the class and the resolved concrete model per spawn, so a post-hoc cost question has the model that actually ran (design.md §Decision 1).
- [ ] 2.4 `workflows/ship.js` + `test/workflows.test.mjs` + regenerate `test/fixtures/prompts/implementer-lane-tier-*.txt`: resolve class → Anthropic slug at each `agent({ model })` call site; rewrite the classifier prompt's tier ladder to name classes instead of `haiku`, satisfying the `ship/prompt-integrity` delta's "no assembled prompt names a vendor model" scenario. Restate the class set as literals here rather than importing — the workflow runtime rejects `import()` — and extend the existing drift test that guards the spawn-prefix literals to cover these too (design.md §Context).
- [ ] 2.5 `lib/host.mjs` + `test/spine/host.test.mjs`: update the `SpawnRequest.model` contract to a class and document that a host resolves it through `lib/models.mjs`. `spawnPrefix` and the four prefix literals are unchanged.
- [ ] 2.6 `lib/host/acp.mjs` + `test/spine/acp-host.test.mjs`: resolve the class to a slug for the `_meta` extension slot. `modelRoutingSupported: false` stays false — ACP still has no per-prompt model selector, and the `model-routing` spec requires a host in that position to say so rather than imply a cost profile.
- [ ] 2.7 `bin/interlock-ship-acp`: consume the class from `laneModel` unchanged; no behavior change beyond the rename.
- [ ] 2.8 `lib/limits.mjs` + `test/spine/limits.test.mjs`: publish the tier→(model, effort) routing table through `formatLimits` so `interlock limits` prints it, giving the `model-routing` spec's "published mapping and dispatched effort cannot disagree" scenario something to compare against.

## 3. Plan reuse fails closed on the new plan shape

- [ ] 3.1 `lib/plan-fingerprint.mjs` + `test/spine/plan-fingerprint.test.mjs`: bump the plan format version so a plan written before section 2 is not reused against a reader expecting classes. No spec delta is needed — `openspec/specs/plan-reuse/spec.md` already requires the fingerprint to record the plan format version and already specifies that a version mismatch forces re-planning regardless of the input fingerprint. Add a regression case asserting a stored plan carrying `model: "sonnet"` is rejected for reuse and the classifier runs.

## 4. The Codex host and the Codex authoring surface

Independent of one another; both depend only on section 1.

- [ ] 4.1 Create `lib/host/codex.mjs` + `test/spine/codex-host.test.mjs`: the third implementation of the `lib/host.mjs` port. `spawn` runs one `codex exec` process per request with the argv from design.md §Decision 3 — `--cd`, `--sandbox workspace-write`, `--model`, `-c model_reasoning_effort=`, `--output-schema <tmp>`, `--output-last-message <tmp>`, `--json`, `--ignore-user-config`, `--ephemeral`, `--skip-git-repo-check`, and `--add-dir` only when the operator asked. Write the schema to a temp file per spawn and read the result from the last-message file, guarding with `parseAgentJson`; a result failing the schema resolves to `null`, matching the workflow runtime's contract. Refuse `--dangerously-bypass-approvals-and-sandbox` with a halt. Do not invent a Codex plugin-agent id — record the sandbox mode, the writable-path set, and `--ignore-user-config` as the constraint actually in force (design.md §Decision 5). Reuse `mapPipeline` and `runCli` from `lib/host.mjs` unmodified; do not throttle batch width. Enforce a transport wall clock whose expiry, a failure to start, and an exit without a usable result all resolve to `null`. Test with a stub `codex` on PATH so the suite needs no network: argv construction per tier, schema rejection → `null`, timeout → `null`, refused bypass mode, and — driving the real `interlock` binary through `runCli` — that a halt comes from the CLI's exit status and no halt rule is duplicated in the host.
- [ ] 4.2 Create `AGENTS.md` at the repo root + a check in `test/spine/` asserting it restates no published cap, tier definition, or limit value: purpose, the test command, the commands an agent may run, and pointers into `docs/` and `interlock limits`. Pointer file only — `lib/limits.mjs`'s header states why a cap in prose is read as guidance, and `codex-surface` makes that a test.
- [ ] 4.3 Create `prompts/codex/interlock-{spec,explore,review-artifacts,ship,commit}.md` and the `interlock codex prompts --install` subcommand in `bin/interlock`, with tests: install into `$CODEX_HOME/prompts/` (defaulting to `~/.codex/prompts/`), namespaced so it cannot collide with an operator's own prompt, idempotent across repeated runs with no version change, leaving unrelated operator prompts byte-identical, and reporting what it added. The prompts and the subcommand are one production path — the install has nothing to copy until the files exist.

## 5. The Codex ship driver

- [ ] 5.1 Create `bin/interlock-ship-codex` and add its `bin` entry to `package.json`: the lean loop — validate, classify, plan, wave loop, verify, commit — structured after `bin/interlock-ship-acp` and driving the same `interlock` subcommands, with every decision arriving as a CLI exit code. Load `assembleImplementerPrompt` and the lane-dispatch helpers from `workflows/ship.js` through the same runtime loaders the ACP driver uses, so prompt assembly and lane dispatch are not duplicated (design.md §Decision 9). Check the `@openai/codex` minimum of `0.149.0` once at startup and halt naming the version found. Probe model reachability on the first spawn and halt naming the model and the authentication mode. Refuse `--strict`, `--review`, `--handoff` and `--conformance` with exit 2, naming each refused flag and executing no wave, and refuse an invocation whole when it mixes supported and unsupported flags. Exit 0 on a terminal summary, 1 on a halt, 2 on an unsupported invocation.

## 6. Drift guards and documentation

- [ ] 6.1 Extend the cross-host classifier tier-policy comparison to all three drivers, per the `ship/prompt-integrity` delta: the comparison covers every driver the repository ships, passes on extracted policy rather than byte equality, and fails naming an unregistered driver rather than silently comparing only the drivers it already knew about.
- [ ] 6.2 `README.md`: replace "Cursor and Copilot are not supported in 0.x … Claude Code is the default and supported host" with a host matrix naming each host and which stages run on it, per `codex-surface`. State the optional `@openai/codex@0.149.0` runtime requirement as Codex-only, and state that the Codex host's sandbox removes the approval-prompt failure the existing allowlist paragraph mitigates on Claude Code. Keep `--strict` documented as Claude Code only.
- [ ] 6.3 `docs/04-when-it-stops.md`: add the Codex halts — version below minimum, unreachable model, refused bypass sandbox mode, refused strict invocation, schema rejection reaching the task-failure cap.
- [ ] 6.4 `docs/10-agentic-workflow-ship-and-spec.md`: close the "No `AGENTS.md` or `CLAUDE.md`" gap at line 34 and the Cursor/host row at line 63; update the file-map table for `AGENTS.md` and `prompts/codex/`.
- [ ] 6.5 `docs/08-harness-landscape.md`: place the Codex host in the existing layer table rather than describing it as a new layer.
- [ ] 6.6 `CHANGELOG.md`: record the Codex host, the Codex surface, and the `model` field's class rename as **BREAKING** for any consumer reading `haiku|sonnet|opus` out of a plan, a next step, or the run log.
