## 1. Receipt fields and the deterministic readers behind them

- [x] 1.1 Add the two path sets to the receipt in `lib/run-log.mjs`. Introduce a `nullableStringArray` coercer beside `nullableCount`/`nullableBool` — `null` for an absent source, a bounded array otherwise — and a module constant for the path-set bound, placed beside the existing field bounds rather than in `lib/limits.mjs` (design D4). Add to the `run-receipt` field table, each copied by name: the touched set, the reason it is unobserved, the predicted set, whether the prediction was complete, and whether either set was truncated. Render them in `receiptLines` as `unknown` when unobserved and never as blank or `[]`, and surface what a reader needs from `readRunLog`'s per-run summary. Cover with tests for: an observed set round-tripping; an absent source reading `null` rather than `[]`; a set past the bound recorded truncated with the append still succeeding; and a fat source object carrying diff hunks and file contents leaking nothing but the named path strings. Satisfies all three `ship-run` requirements.
- [x] 1.2 Add a deterministic reader for a commit's touched paths — a new small module in `lib/` that, given a repo root and a commit identifier, returns the repository-relative paths that commit touched, following the `execFileSync('git', …)` precedent at `lib/drift.mjs:60`. It returns an observed set or an explicit unobserved result carrying the reason; it never throws, never returns a partial list, and never returns `[]` for a failed read (design D1, D2). Test a real commit in a temp repository, a missing identifier, a non-repository root, and a git invocation that fails.
- [x] 1.3 Add a deterministic reader for the executed plan's predicted paths: the union of `task.paths` across the executed plan's tasks, plus whether every task declared paths. `lib/waves.mjs:209` documents `paths` as optional by design, so a plan where any task declined must report the prediction incomplete, and a plan that could not be read at all must report both the set and its completeness unobserved (design D3). Test a fully-predicting plan, a plan with one silent task, an empty plan, and an unreadable plan.
- [x] 1.4 Add the capture module in `lib/`: given a repo root, a run id and an optional task id, build an eval-case skeleton. Refuse when `checkRunLog(root, runId)` reports the run not reconstructable, reporting that function's own problem list verbatim — no second notion of reconstructable (design D7). Derive the provenance from the trajectory locator and the cited event range; quote one observed out-of-set or fabricated value from those events as the deterministic grader's pattern, marked as requiring confirmation; emit a stated, marked placeholder and say so when no value can be quoted; emit a stated placeholder for the prompt when the trajectory recorded none; never emit a judged grader and never emit the per-pull-request tag. Test each refusal path, both grader paths, and that the emitted files carry a pinned schema version, a non-empty provenance line and a known grader type.
- [x] 1.5 Add the eval-prerequisite check to `lib/doctor.mjs`: when an eval suite exists at the plugin root, one row for the harness's early-access enablement and one for a model credential; no rows at all when no suite exists. Status is only ever satisfied or `skip` — the check catches its own errors and degrades to `skip` with the reason rather than inheriting the `fail` that `run(id, fn)` produces from a thrown probe (design D11). Read presence only; never read, print or record a credential value in the human or JSON surface (design D12). Test: both rows present with a suite and absent without one; an absent prerequisite reporting `skip` and not `fail`; a throwing probe reporting `skip`; an otherwise-clean preflight still exiting zero with both prerequisites absent; an unrelated failing check still failing with the eval rows present; and no credential value in either output surface. Satisfies all four `evals/prerequisites` requirements.

## 2. The CLI surface and the computed indicator

- [x] 2.1 Expose the two readers from 1.2 and 1.3 through `bin/interlock` so a host can obtain both sets deterministically at run close, add them to the help text, and keep every existing exit code unchanged. A read that fails prints its reason and yields an unobserved result rather than exiting non-zero — the receipt must still be written. Test both success and every unobserved path through the real binary. Depends on 1.2 and 1.3.
- [x] 2.2 Wire `evals capture --run <runId> [--task <id>] --out <dir>` into `bin/interlock`'s `evals` case beside `triage`, extending the `unknown evals subcommand` message. Refuse an output directory that resolves inside `evals/`, refuse a directory that already holds a case, exit non-zero on every refusal with the reason, and write nothing on any refusal. Add the command to the usage block and its refusal exit to the exit-code table. Test each refusal, a successful emit into a temp directory, and that nothing under `evals/` is created or modified in any case. Satisfies the `evals/capture` requirements on refusal and write location. Depends on 1.4.
- [x] 2.3 Compute the indicator in `lib/report.mjs`. Replace `planFidelity.diffMatchesPlan`'s `{computable: false, reason, wouldRequire}` with a computed share pooled over paths — touched paths that were predicted, over touched paths, summed across qualifying runs (design D6) — carrying its denominator, the count of qualifying runs, and a count per exclusion reason (no commit, unreadable set, incomplete prediction, truncated set). A zero denominator yields unobserved with a reason, never zero. Update the `NOT COMPUTABLE` rendering block and the module-header paragraph that declares the gap. Add no gating, no verdict and no threshold — `report/non-gating` binds unchanged and gets no delta (design D8). Test: a computed share with its denominator; each exclusion reason counted and named; an all-excluded corpus rendering unobserved rather than zero; and that the converse figure is not published under the same name. Satisfies both `report/indicators` requirements. Depends on 1.1.

## 3. Hosts, skill and documentation

- [x] 3.1 Populate the two sets at run close in `workflows/ship.js`: obtain the touched paths from the commit identifier the run already records and the predicted paths from the executed plan, pass both into the receipt by name alongside the existing summary fields, and record unobserved with a stated reason on every path where either is unavailable — including a run that made no commit, a run whose commit step reported failure, and a run whose plan could not be read back. The sets come from the deterministic readers, never from the commit agent's response (design D1). Extend the ship-harness assertions to cover the receipt carrying both sets on a committing run and both unobserved on a halted one. Depends on 2.1.
- [x] 3.2 Do the same for `bin/interlock-ship-acp`, or record both sets unobserved with a reason naming the host's inability where it cannot supply them — the declared per-host difference, in the shape the token-spend fields already use. Test the host's receipt on both paths. Depends on 2.1.
- [x] 3.3 Update `skills/evals/SKILL.md` §1 to name a captured skeleton among the admissible evidence, state that it is a draft rather than a finished case, and require every value the capture marked as requiring confirmation to be resolved before a case is authored. Pin the addition in `test/skills.test.mjs` following the `test/skills.test.mjs:195` precedent — assert distinguishing tokens, not sentences, so a reword does not delete the pin. Satisfies the `evals/authoring` requirement. Depends on 2.2 for the command the instruction names.
- [x] 3.4 Write `docs/14-evals.md` (design D9) covering, for a repository running the spec and ship workflows: which properties of a run are checked and by which command's exit code, the preflight distinguished from the in-run checks; what is recorded, where, and that nothing recorded changes a gate, linking `docs/11-the-indicators.md#whether-to-keep-them` rather than restating it; that Interlock runs no model evals in the consumer's CI, with the three reasons; and how to file a failure via a captured skeleton. Name the command that publishes any cap; state no cap's value. Add the row to `README.md`'s doc table, the link from its Experimental section, and a cross-link from `docs/11`. Satisfies the first five `evals/consumer-posture` requirements. Depends on 2.2 and 3.3.
- [x] 3.5 Pin the page's claims with a doc-claim test following the `test/workflows.test.mjs:393` precedent (design D10): token assertions for each required claim, an assertion that the README link exists, a negative assertion that the page never states that Interlock runs model evals in a consumer's CI, an assertion that no bare numeric threshold appears on the page as policy, and a failure — not a skip — if the page is absent. Satisfies the last `evals/consumer-posture` requirement. Depends on 3.4.

## 4. Cross-surface verification

- [x] 4.1 Add one end-to-end assertion that the unobserved-versus-empty distinction survives every hop: a fixture run that commits and a fixture run that does not, each written to a trajectory, read back through the run-log reader, and reported — asserting that the committing run contributes to the share and the non-committing run is excluded and counted by reason, and that no surface anywhere renders the non-committing run's touched set as an empty list. Depends on 3.1.
- [x] 4.2 Run `npm test` and paste the output. Fix any failure in the code, never by editing, weakening or skipping a test.
