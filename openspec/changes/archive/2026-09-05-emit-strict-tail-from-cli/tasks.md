## 1. Prompt modules for the tail

- [x] 1.1 Create `lib/prompts/review.mjs` exporting `assembleReviewPrompt({ change, dimensions, policyProse, findingsPath, verdictsPath, stageLine })` from `workflows/ship.js:2061-2088`, with each `{ name, rubric }` inlined under its own heading, a null rubric rendered as "works from the dimension name alone", the policy prose prepended to every reviewer's criteria, the two-skeptics and cited-dismissal instructions kept verbatim, the `interlock review` invocation removed (the CLI adjudicates), and one sentence allowing the agent to add a dimension with a reason (design D3, D4).
- [x] 1.2 Create `lib/prompts/remediate.mjs` exporting `assembleRemediatePrompt({ change, round, plan, dimensions, policyProse, stageLine })` for a fixing round (from `workflows/ship.js:2152-2168`: one fixer per `byFile` group, unscoped last, never fix a suggestion, re-review only `reReviewDimensions` with the same inlined criteria, a dimension that did not run in the first pass given criteria or marked not applicable) and `assembleVerdictPrompt({ change, round })` for the verdict round (fixes nothing, reports counts), with the `interlock remediate` invocation and the `roundCap` copying removed (design D2).
- [x] 1.3 Create `lib/prompts/handoff.mjs` exporting `assembleHandoffPrompt({ change, needsManualTestPlan, testPlanReason, conformance: scenarios | null, learnings: boolean })` from `workflows/ship.js:2295-2328`, with the `interlock surface` and `interlock conformance` invocations replaced by the inlined decision and scenario list (design D5); and `lib/prompts/dimensions.mjs` exporting `selectDimensions(changedPaths)` per design D4 using the path classification in `lib/surface.mjs` / `lib/risk.mjs`, returning `{ dimensions, reasons }`.
- [x] 1.4 Register the three assemblers in `lib/prompts/index.mjs` with sample inputs so `test/spine/prompt-integrity.test.mjs` coverage grows by three; remove the autonomy-record paragraph from `lib/prompts/commit.mjs` (design D6); extend `test/spine/prompts.test.mjs`: criteria inlined per dimension, null rubric sentence, policy prose prepended, no `interlock review`/`remediate`/`surface`/`conformance` invocation in any tail briefing, the verdict variant fixes nothing, the handoff omits the test plan when not needed, `selectDimensions` always returns the four, adds `devops` for a CI workflow path, adds `security` for an auth path, and adds nothing for a docs-only path.

## 2. Tail steps in the run program

- [x] 2.1 In `lib/run.mjs`, replace the `host-tail` emission with the tail program (design D1): at waves `done` with `review` set, emit `review` — dimensions from `selectDimensions` over the observed changed paths, rubrics read through the reader `interlock review` uses and policy prose through the review-policy parser, `REVIEW RUBRIC UNAVAILABLE` banners for null rubrics, effort `EFFORT.skeptic` from `lib/limits.mjs` (design D8), the `review` stage line, `then: run reviewed`; add `interlock run reviewed --results <file> --json`: adjudicate `findings.json`/`verdicts.json` with `lib/review-core.mjs` using the observed changed paths, write `review.json` and the metrics file, record agent-added dimensions on the manifest, plan round one with `lib/remediate.mjs`, and emit `remediate` (round plan inlined, `remediation` stage line) or `verify-final`; add `interlock run remediated --round N --results <file> --json`: re-adjudicate, record `fixed`/`deferred` and `fixRoundsRun` on the manifest, and emit the next fixing round, the verdict step, `verify-final` after a clean verdict, or `halt` naming the surviving blocker count (design D2).
- [x] 2.2 In `lib/run.mjs`, after `run judge --context final` passes: when `handoff` or `conformance` is set, run `interlock surface --changed <observed>` and (if `conformance`) `interlock conformance <change> --changed <observed>` in-process, emit `handoff` with the inlined decision and scenarios, `then: run commit` (design D5); in `run close`, when the manifest is strict, record the review-code autonomy outcome with the CLI's surviving-blocker count (design D6); in `lib/receipt.mjs`, read `review`, `remediation`, `remediationRounds`, handoff counts and the autonomy outcome from the manifest and work files (design D7), keeping the `LEAN SHIP` line's condition unchanged.
- [x] 2.3 Update `bin/interlock`'s `run` usage text for `reviewed` and `remediated`; extend the sequence guard so tail continuations are enforced.
- [x] 2.4 In `test/spine/run.test.mjs`, add the strict path with fixture `findings.json`/`verdicts.json` sets: the step order under `--strict`; the review briefing inlines every selected dimension's criteria and the policy prose; a missing rubric file yields the null-rubric sentence and the `REVIEW RUBRIC UNAVAILABLE` banner; one fixing round clearing blockers yields the verdict then `verify-final`; blockers surviving the verdict yield `halt` naming the count and no `commit`; raising `LIMITS.remediationRounds` by one in a stubbed limits adds one fixing round with no driver change; `--handoff` alone emits `handoff` and no `review`; an agent-added dimension is adjudicated and recorded; the commit briefing carries no autonomy instruction; `run close` on a strict run records the autonomy outcome (zero on clean, one on a halted verdict) and on a lean run records nothing; tail continuations are sequence-guarded.

## 3. Drivers

- [x] 3.1 In `workflows/ship.js`, delete the `HOST_TAIL_SEAM` sections (review, remediation, handoff), the `REMEDIATION_BUDGET` block and `remediationBudget`, `RUBRIC_INSTRUCTIONS`, `POLICY_INSTRUCTIONS`, `SKEPTIC_EFFORT`, `VERIFY_EFFORT` and any helper now unreferenced; the interpreter loop is unchanged.
- [x] 3.2 In `bin/interlock-ship-acp`, delete `REFUSED_FLAGS` and the exit-2 branch, pass `--review`/`--handoff`/`--conformance`/`--strict` through to `run start`, and rewrite the usage text's "Not supported on this host" block to say the tail runs on this host; remove the `host-tail` handling.
- [x] 3.3 In `test/workflows.test.mjs`, remove the `HOST_TAIL_SEAM` allowance from the no-policy sweep and add tail tokens (`isReal`, `qualityScore`, `reReviewDimensions`, `roundCap`, `byFile`, `needsManualTestPlan`, `autonomy record`); add a driver-level test that `bin/interlock-ship-acp <change> --strict` against the fixture agent (returning canned counts and writing fixture findings/verdicts) reaches `review` and closes with `0` or `1`, never `2`; in `test/skills.test.mjs`, retarget the pin on `skills/ship/SKILL.md` §3 so it asserts the ACP binary runs the same loop including `--strict` and no longer asserts "stays Claude Code only".

## 4. Docs and skills

- [x] 4.1 Update `skills/ship/SKILL.md` §3 (the ACP binary runs the full loop; it is still never invoked by the skill), `README.md` Experimental (remove the "Lean only" bullet; state that review, handoff and conformance run on the second host from CLI-emitted steps), `docs/04-when-it-stops.md` (drop the exit-2 refusal paragraph; exit `2` retained for unsupported invocations only), `docs/06-why-it-works.md` §7 opening (the tail is CLI-emitted; adjudication and the budget never leave the CLI), `docs/10-agentic-workflow-ship-and-spec.md` §4 (`--strict` sequence), and add a `CHANGELOG.md` entry.

## 5. Validate

- [x] 5.1 Run `openspec validate emit-strict-tail-from-cli --strict`, `interlock validate emit-strict-tail-from-cli` and `interlock ledger emit-strict-tail-from-cli`; fix anything reported.
- [x] 5.2 Run `npm test` and `npm run validate` and confirm both are green, including the no-policy sweep with no allowance.
