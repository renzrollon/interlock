## 1. Publish the caps and switch every reader

- [x] 1.1 In `lib/limits.mjs`, add `LANE_CAPS = { byTier: {1: 8, 2: 8, 3: 6, 4: 4, 5: 8}, cohesionMaxTier: 3 }` and `SOLO = { maxTasks: 20 }` as separate exports beside `EFFORT` (design D2, ledger D2/D5), remove `LIMITS.maxTasksPerAgent`, and extend `formatLimits` with one row per tier cap, the cohesion tier ceiling and the solo envelope. Document in the file comment why the scalar is gone rather than aliased.
- [x] 1.2 In `test/spine/limits.test.mjs`, add `LANE_CAPS` (tokens `LANE_CAPS.byTier`, `LANE_CAPS.cohesionMaxTier`) and `SOLO` (`SOLO.maxTasks`) groups to the cap-authority reader check, pin the design's values the way the effort table is pinned, assert `formatLimits` prints all three, and drop every assertion that names `maxTasksPerAgent`.
- [x] 1.3 In `test/workflows.test.mjs`, retarget the pin around line 1285 ("the lane cap is stated once, read by the planner, and never restated in the script") from `maxTasksPerAgent` to `LANE_CAPS`: `lib/waves.mjs` must read `LANE_CAPS.byTier`, `lib/limits.mjs` must define it, and `workflows/ship.js` must still not carry the lane cap at all.
- [x] 1.4 Check the `workflow-authoring` reference for a per-agent turn or token budget option on `agent()`. If one exists, add a published allowance to `SOLO` in `lib/limits.mjs` (with a reader and a limits row) and pass it at the implementer spawn in `workflows/ship.js` scaled by lane length; if none exists, record that finding under Risks in this change's `design.md`. Either way, state the outcome in the task's handoff.

## 2. Planner: cohesion lanes, solo planning, run state, fingerprint

- [x] 2.1 In `lib/waves.mjs`, replace the scalar lane cap with a per-tier lookup (`LANE_CAPS.byTier[laneTier(lane)]`, tier 0 uses tier 1, `opts.maxTasksPerAgent` applied as a uniform ceiling) and add cohesion packing after collision components form: next-fit over hardest-first components whose tier is at or below `LANE_CAPS.cohesionMaxTier`, tasks in the finished lane sorted by id, applied per dependency layer to implementation groups and to the trailing test wave (design D1, D3, D4). Give every entry in `lanes[]` a `kind` of `collision` or `cohesion`, print cohesion lanes distinguishably in `formatPlan`, and emit a warning per cohesion fold naming the ids, the tier and the cap. Carry `laneCaps` on the run state in place of `maxTasksPerAgent`, read it in `makeWave`, and fall back to `LANE_CAPS.byTier` for a state written without it (design D10).
- [x] 2.2 In `lib/waves.mjs`, add solo planning on top of 2.1: `planWaves` reads `opts.mode` and the classification's top-level `recommendedMode` / `modeReason`, decides per design D6 (flag, then classifier inside `SOLO.maxTasks`, then waves, contradictory input rejected), and in solo mode emits one wave of one batch of one lane holding every implementation task in (group, layer, id) order followed by every test task in (layer, id) order with `testWave` null (design D5, D8). After the clamp, promote every task's model to opus and record `promoted[]` shaped like `clamped[]`, omitting tasks already on opus (design D7). Report `mode`, `modeSource`, `modeReason` (bounded to `LIMITS.maxHandoffChars`) and `kind: 'solo'` on the lane; `formatPlan` opens with the mode line and lists promotions beside clamps; emit the D6 warnings. Store `mode` on the run state (default `waves` when absent) and include it in every `nextStep` payload.
- [x] 2.3 In `lib/plan-fingerprint.mjs`, bump `PLAN_FORMAT` to `interlock.ship-plan/3`, make `capsOf` read `LANE_CAPS` and `SOLO` plus `opts.mode` (`auto` when absent), add the canonical lines `mode`, `lane-caps`, `cohesion-max-tier` and `solo-max-tasks` after the existing cap lines, print them in `formatFingerprint`, and make `narrowPlan` explicitly preserve `mode`, `modeSource` and `promoted` (design D11).
- [x] 2.4 In `bin/interlock`, after 2.2 and 2.3: accept `--mode solo|waves` on `waves`, `plan reuse` and `plan fingerprint` and pass it through; have `wave-state create` carry the plan's mode and `laneCaps` into the state and `wave-state next` echo `mode` in its JSON; emit `laneCaps` and `solo` as their own groups in `limits --json`; update the USAGE text for each flag.
- [x] 2.5 In `test/spine/waves.test.mjs`, cover every scenario in the `lanes` and `solo-mode` deltas: seven tier-2/3 disjoint tasks become one cohesion lane; tier-4 siblings stay separate; a cap closes a lane and the remainder opens another; test-wave cohesion; a dependent task never shares a lane or batch with its dependency; two sections never share a lane; per-tier cap by hardest tier; override of 1 reproduces one agent per task; solo shape and order including an edge that overrides id order and a test-only change; mode precedence for flag, classifier inside and outside the envelope, absent recommendation, and contradictory input; promotion report with an already-opus task omitted; run-state `mode` and `laneCaps` fallbacks; and a replay-determinism check that the same classified input plans byte-identically twice. Retarget the existing `maxTasksPerAgent` tests to the table and the override.
- [x] 2.6 In `test/spine/plan-fingerprint.test.mjs`, assert the format bump rejects a `/2` plan, the four new canonical lines appear, a mode override changes the hash while an identical override matches, `recommendedMode` does not affect the hash, and a narrowed solo plan keeps one lane, its mode and its promotions.
- [x] 2.7 In `test/spine/cli.test.mjs`, assert `interlock waves --mode solo` yields a one-lane plan, both modes are rejected together, `limits --json` carries `laneCaps` and `solo`, and `wave-state next` echoes `mode`.

## 3. Prompts, hosts and flags

- [x] 3.1 In `workflows/ship.js`: extend `parseInvocation` with `--solo` / `--waves` into `laneMode` and halt at parse on both; thread `--mode` into the `plan-reuse` probe and the `plan-waves` step's `interlock waves`, `plan fingerprint` and `wave-state create` commands; add the MODE paragraph to the classifier prompt asking for top-level `recommendedMode` and `modeReason`, describing solo as one agent implementing the whole change in order, naming the published envelope as the planner's to enforce and never stating its number; in `assembleImplementerPrompt` reword the multi-task heading so it no longer claims the tasks share files, add a `solo` input that renders the whole-change heading and forces the full-read ladder without changing task tiers (design D9); pass `solo: next.mode === 'solo'` at the implementer spawn. The script must not carry the lane cap or the envelope: it dispatches the lanes the planner built.
- [x] 3.2 Regenerate the five `test/fixtures/prompts/implementer-lane-tier-*.txt` fixtures for the reworded heading, add five `implementer-solo-tier-*.txt` fixtures, and extend `test/spine/implementer-prompt.test.mjs` to pin them plus the three properties the delta names: the single-task snapshots are unchanged, the lane heading does not claim shared files, and a solo lane of tier-2 tasks is briefed at the full-read ladder while the same lane without the flag is not.
- [x] 3.3 In `test/spine/planner-prompt.test.mjs`, assert the assembled `plan-waves` prompt asks for `recommendedMode` and `modeReason`, describes solo as one agent in order, and does not contain the value of `SOLO.maxTasks` as a solo bound.
- [x] 3.4 In `test/workflows.test.mjs` and `test/helpers/ship-harness.mjs`, cover `parseInvocation` for `--solo`, `--waves`, neither and both; assert the `--mode` flag reaches the `plan-reuse`, `waves`, `plan fingerprint` and `wave-state create` commands in the captured prompts; and assert the implementer spawn passes `solo` when the stubbed `wave-state next` reports `mode: solo` and omits it otherwise.
- [x] 3.5 In `bin/interlock-ship-acp`, pass `solo` from the step payload's `mode` at its assembler call, and cover it in `test/spine/acp-host.test.mjs`.

## 4. Skills, docs and changelog

- [x] 4.1 In `skills/ship/SKILL.md`, add `--solo` and `--waves` to the argument hint and the flag table (`flags: ["solo"]` / `["waves"]`), state in one sentence that the plan preview names the mode before anything runs, and pin the new tokens in `test/skills.test.mjs`.
- [x] 4.2 In `skills/spec/SKILL.md` under "Task shape for ship", add the rule that low-tier siblings in one section are packed into one lane and a small change may ship solo, so a section is never split to buy parallelism; pin the distinctive tokens in `test/skills.test.mjs`.
- [x] 4.3 Update `docs/10-agentic-workflow-ship-and-spec.md` (the wave-loop step that says one agent per task, the token-tactics list, the flag reference), the lane paragraph in `docs/06-why-it-works.md`, the ship flags in `README.md`, and add a `CHANGELOG.md` entry naming cohesion lanes, the per-tier cap table, solo mode and the plan-format bump.

## 5. Validate

- [x] 5.1 Run `openspec validate add-lane-cohesion-and-solo-mode --strict`, `interlock validate add-lane-cohesion-and-solo-mode` and `interlock ledger add-lane-cohesion-and-solo-mode`; fix anything reported.
- [x] 5.2 Run `npm test` and `npm run validate` and confirm both are green, including the regenerated fixtures and the cap-authority groups.
