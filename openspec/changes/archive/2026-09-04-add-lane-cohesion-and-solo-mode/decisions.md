# Decisions — add-lane-cohesion-and-solo-mode

| id | question | class | resolution | evidence |
|----|----------|-------|------------|----------|
| D1 | Which tasks are cohesion-eligible: by tier, by a classifier hint, or everything under `maxParallel`? | agent_resolved | By tier only: components whose hardest tier is at or below `LANE_CAPS.cohesionMaxTier` (3). No classifier hint in this change; tier 4–5 stay isolated. | user instruction 2026-09-04 ("no thin subagent spawn for minor tasks"); `lib/waves.mjs:10-14` (classifiers already mis-set one field); design.md D1 |
| D2 | What are the per-tier lane caps, and does the tier-5 (opus) cap rise? | agent_resolved | `{1: 8, 2: 8, 3: 6, 4: 4, 5: 8}`; the scalar `LIMITS.maxTasksPerAgent` is removed and every reader switches to the table; the uniform override of 1 remains the rollback lever. | user instruction 2026-09-04 ("increase the subtasks count of some opus workers to 8 or 12"); `openspec/changes/archive/2026-08-23-fold-serial-batches-into-lanes/design.md` D5 and Open Questions; design.md D2 |
| D3 | Next-fit or first-fit packing, and what order inside a cohesion lane? | agent_resolved | Next-fit over hardest-first components; tasks inside the finished lane in task-id order like collision lanes. | `lib/waves.mjs:485-499` (hardest-first ranking), `lib/waves.mjs:358-361` (`byTaskId`); design.md D3 |
| D4 | Does cohesion apply to the trailing test wave? | agent_resolved | Yes, per layer under the same gate; the deferral is about when tests run, not how many agents run them. | `lib/waves.mjs:727-745` (test-wave chunking comment); design.md D4 |
| D5 | Is the solo envelope task count alone, or count plus shape (sections, files)? | agent_resolved | Task count alone (`SOLO.maxTasks` = 20); the classifier's recommendation supplies the shape judgment inside it. | user instruction 2026-09-04 ("20 or less ... not hard rule"); design.md D6 |
| D6 | When the flag, the classifier and the envelope disagree, which wins? | agent_resolved | Flag, then classifier inside the envelope, then waves. A flag may exceed the envelope with a warning; contradictory flags halt at parse. | `workflows/ship.js:60-88` (`parseInvocation` precedent), `lib/limits.mjs` `clampParallel` (a request is a preference); design.md D6 |
| D7 | Does a solo lane always run on opus, and how is that reconciled with the clamp? | agent_resolved | Always opus, by planner promotion after the clamp, reported as `promoted`; effort stays the published tier table; briefing tier is forced to full-read without rewriting task tiers. | user instruction 2026-09-04 ("one opus subagent"); `lib/waves.mjs:90-97` (`clampModel` guards the classifier, not the planner); design.md D7 |
| D8 | Do test tasks join the solo lane or stay a separate trailing wave? | agent_resolved | They join, last, in the same agent; final unit verify and `rootCauseIterations` still own repair. | `lib/waves.mjs:727-745`, `lib/limits.mjs` `rootCauseIterations`; design.md D8 |
| D9 | Reword the multi-task heading (breaking five lane fixtures) or add a lane-kind field to the run state? | agent_resolved | Reword once so it is true for collision and cohesion lanes, regenerate the five lane fixtures deliberately; solo gets its own heading via a `solo` flag read from the run state's `mode`. Single-task fixtures untouched. | `openspec/specs/implementer-prompts/spec.md` (only single-task byte-identity is required), `test/spine/implementer-prompt.test.mjs:1-10`; design.md D9 |
| D10 | Should a solo run be replannable? | agent_resolved | A solo run has one wave; once executed there is nothing to revise, so `replan` cannot fire. Before execution a replan re-lanes the whole change as one solo lane, reading mode from the state. | `lib/waves.mjs:1543-1556` (`revisableGroups`), design.md D10 |
| D11 | Is the classifier's `recommendedMode` hashed into the fingerprint? | agent_resolved | No. It is an output of planning; only the flag (`auto` when absent), the cap table and the envelope are hashed. Plan format bumps to `/3`. | `lib/plan-fingerprint.mjs:120-200` (caps are hashed, classifier output is not); design.md D11 |
| D12 | Does a per-agent turn ceiling in the workflow runtime bound a solo lane, and should the plugin pass one? | agent_resolved | Unknown from the code and the workflows page; task 1.4 checks the workflow-authoring reference and either wires a published allowance or records that none exists. Not a product decision. | `workflows/ship.js:1664-1678` (no turn option passed); design.md Risks |
