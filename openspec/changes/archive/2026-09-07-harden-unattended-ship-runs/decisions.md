# Decisions — harden-unattended-ship-runs

| id | question | class | resolution | evidence |
|----|----------|-------|------------|----------|
| D1 | Where does the push live — a plugin Stop hook, the decision module, a driver-executed step, or the CLI close? | agent_resolved | A `--notify` flag on `interlock run close`, passed from both drivers' `closeArgs()`, with the POST in a new `lib/notify.mjs`. No Stop hook in this change: it could never cover `bin/interlock-run`, the stage marker is cleared before it fires, and Stop-after-Workflow timing is undocumented. | `workflows/ship.js:36-38`; `test/workflows.test.mjs:60-67`; `bin/interlock-run:438-445`; `lib/ship-stage.mjs:198`; `docs/08-harness-landscape.md:769`; `docs/04-when-it-stops.md:134`; explore brief §Options 1; design.md D1 |
| D2 | Where is the topic configured — a settings.local.json key (the ideas doc's sketch), an env var, or a new Interlock file? | agent_resolved | `INTERLOCK_NTFY_TOPIC` and `INTERLOCK_NTFY_URL` (default `https://ntfy.sh`); nothing read from the tree. Env is the repo's only config convention, the topic is a per-user capability, and a typo in a settings file would surface as a permissions failure. | `lib/host/claude-cli.mjs:45-51`; `lib/doctor.mjs:277-281`; `hooks/preflight.mjs:65-81`; `workflows/ship.js:372` (the ping already reads env); design.md D2 |
| D3 | Push only on halt, or on every terminal close? | agent_resolved | Every terminal close; ntfy priority `high` on a halt, default otherwise. The person who walked away needs the outcome either way. | ideas doc §Quick wins ("while you are out"); `docs/08-harness-landscape.md:769`; design.md D3 |
| D4 | What does the message carry, given the relay is public by default? | agent_resolved | The summary's first line as title, the change and run id as body; never the topic, cwd, slug, diffs or finding text. | `lib/receipt.mjs:286-290`; explore brief §Risks (public relay); design.md D4 |
| D5 | How is the push outcome spoken without nagging users who never configured it? | agent_resolved | Configured: `push:` summary row, plus a `PUSH FAILED:` degradation banner on failure; unconfigured: no row, and `interlock doctor` reports `notify: skip`. Bounded by `LIMITS.notifyTimeoutMs`; exit code never changes. | `CLAUDE.md` §Things to get right (degradation spoken; corpus-loss semantics); `lib/receipt.mjs:235-265`; `lib/doctor.mjs:336-340` (skip status); `lib/limits.mjs:1-14`; design.md D5 |
| D6 | Include the spec-checkpoint push (`skills/spec` §6 calls `interlock notify checkpoint <change>`, pinned in `test/skills.test.mjs`) in this change, or keep it ship-only? | needs_human | Recommended: include. The ideas doc calls this quick win "idea 1's sender" and idea 1's first story is the checkpoint push; cost is one skill line, one CLI sugar and one token pin (task 3.2). If ship-only, delete task 3.2 and the last requirement of `specs/ship/close-notification/spec.md`. | ideas doc §1 Checkpoint story 1 and §Quick wins; design.md D6 |
| D7 | What does docs/10's "blocking reminder" mean at ship close — a verbatim line or a non-zero exit? | agent_resolved | A verbatim `ARCHIVE PENDING — <change>: after merge, run openspec archive <change>` line (plus an `also unarchived:` count), exit code untouched. The shipped change always satisfies the unarchived predicate at close, so exit 1 would halt every clean ship; four authorities forbid a drift-derived gate. | `lib/drift.mjs:90-93` vs `lib/run.mjs:2385-2392`; `docs/14-evals.md:53-57`; `lib/drift.mjs:27-36`; `bin/interlock:1536`; `test/spine/cli.test.mjs:2102-2119`; `test/spine/run.test.mjs:203`; `docs/10-agentic-workflow-ship-and-spec.md:183,445`; design.md D7 |
| D8 | Which module owns the archive check — the receipt or the close — and does the close shell out to `interlock drift`? | agent_resolved | `runClose` calls `detectUnarchived(root)` directly and passes a result object; `formatRunSummary` owns the wording via an optional, defaulted input. No re-exec of `interlock drift`. | `lib/receipt.mjs:1-11` (zero imports); `CLAUDE.md` §Architecture (lib is pure); `lib/run.mjs:2385` (precedent read); `test/workflows.test.mjs:2289-2293`; design.md D8 |
| D9 | How is the project slug derived, and what prints when a run has no run id? | agent_resolved | `projectSlug(absPath)` replaces every character outside `[A-Za-z0-9]` with `-`, over the resolved root the close ran in; `run: none — the run halted before a plan was adopted` when the manifest never adopted a plan. New pure module `lib/project-slug.mjs`. | `~/.claude/projects` listing on the authoring machine (`/`, `.`, space, `_` observed); `lib/run.mjs:259,1528`; `lib/receipt.mjs:92-99`; design.md D9 |
| D10 | Also record run id / cwd / slug in the receipt JSON and outcome record, or capture the Claude Code session id? | agent_resolved | No, in this change. The run id is already the trajectory filename and envelope; cwd on the receipt needs a six-site whitelist sweep and a spec delta; the outcome field list doubles as the agent refusal list; session-id capture rests on an undocumented Workflow/session relationship. | `lib/run-log.mjs:275-282,315-345,490-495`; `lib/outcomes.mjs:67-75`; `hooks/preflight.mjs:1-104` (reads no stdin); design.md D10 |
| D11 | Commit the untracked `.claude/settings.json`, and keep `Bash(git:*)` or narrow it? | agent_resolved | Commit it as-is (plus D12's three rules); keep `Bash(git:*)` — the matcher refuses narrower rules and the docs endorse the breadth. | `git status` (`?? .claude/settings.json`); `.gitignore:18`; `lib/doctor.mjs:230-247`; `docs/04-when-it-stops.md:100`; `test/spine/package.test.mjs:122-131`; design.md D11 |
| D12 | The six rules do not make a run prompt-free — extend the doctor's required set, or rewrite the probe? | agent_resolved | Extend `REQUIRED_COMMANDS` with `test`, `printenv`, `mkdir`, add the rules here, and pin the claim with a repo-fact test and a drift test over the driver and briefing text against a dated `HOST_READ_ONLY` list. Rewriting the probe is a driver behaviour change deferred to a follow-up. | `workflows/ship.js:365-393`; `lib/prompts/stage.mjs:44`; Claude Code permissions doc §Read-only commands (read 2026-09-07); `lib/doctor.mjs:230-238`; design.md D12 |
| D13 | One change or four, and how are tasks grouped when three concerns edit the same two functions? | agent_resolved | One change; tasks grouped by file — leaf modules, settings and docs in section 1, `runClose`/CLI/drivers in section 2, cross-cutting pins in section 3. | `openspec/config.yaml` rules.tasks; explore brief §Recommended Direction; design.md D13 |
| D14 | `runClose` is synchronous and `bin/interlock` has no `await`; how does an async POST fit? | agent_resolved | `runClose` becomes `async`; its single caller awaits it; `main()` becomes async and the entry point becomes `main().catch(...)` with today's handler body. No other caller exists. | `bin/interlock:1437,2376,2515-2521`; `grep -rn "runClose(" test/ lib/ bin/` (one site); design.md D14 |
