## 1. The reader and its CLI surface

- [x] 1.1 Add `lib/report.mjs`. Export `REPORT_SCHEMA`, `buildReport(root, opts)` and `formatReport(report)`. It reads `.claude/learning/outcomes.jsonl` through `readOutcomes` (`lib/outcomes.mjs`), the trajectories through `listRunLogs` / `readRunLog` (`lib/run-log.mjs`), and `.claude/metrics/*.json` directly — classifying each metrics file by its `schema` key, never by filename, because `skills/review-artifacts/SKILL.md:104` writes `review-artifacts-<change>-<stamp>.json` which the glob `review-*.json` also matches while carrying finding bodies instead of counts. Never throws: a missing corpus, an unreadable file and a torn line each become a reported condition with its reason. Writes nothing — no cache, no index. Accepts `{ since, change, maxRuns }` and reports the filter it applied and any scan truncation. Module header states the exit-status prohibition and cites §4.15(a), in the shape of `lib/drift.mjs:27-35`.
- [x] 1.2 Compute the six indicator groups in `lib/report.mjs`, each carrying `source`, `value`, `observedOf` and `notObserved`, with a zero denominator yielding `null` plus a `reason` rather than `0`: **coverage** (trajectories total / with `run-start` / with a terminal event / with a receipt; outcome records by mode; metrics files recognized vs unrecognized by name; skipped lines); **firstPassShip** (receipts reporting not-halted and zero remediation rounds — never labelled a CI rate, since no CI result is recorded); **rework** (remediation-round distribution from receipts, and attempts-per-change from grouped runs, reported separately and never summed, with the `unnamed` share stated); **planFidelity** (recorded `planStatus` distribution using the `REUSE_*` vocabulary from `lib/plan-fingerprint.mjs:60-67`, mid-run revision share from `wave-action` events whose `source` is `replan`, and `diffMatchesPlan` emitted as `{ computable: false, reason, wouldRequire }`); **reviewFindings** (review-metrics counts and receipt counts as two separate series, plus a dismissal share only where `raised` is non-zero); **gateExitHealth** (non-zero `cli-exit` counts grouped by `command`, absent rather than zero for commands with no recorded exits).
- [x] 1.3 Wire `report` into `bin/interlock`: `interlock report [--json] [--since <iso>] [--change <name>]`, its `USAGE` entry, and a line in the `USAGE` exit-code table stating it never exits non-zero. No `process.exit(1)` on any path — follow the `drift` and `conformance` cases at `bin/interlock:1055-1077`, whose comments name the reason rather than leaving the absence to be read as an oversight.
- [x] 1.4 Publish the scan cap through `interlock limits` (`lib/limits.mjs`), following the existing output shape so no bound is restated in prose (`add-interlock-evals` D14).
- [x] 1.5 Unit-test in `test/spine/report.test.mjs` per the existing spine conventions, against fixture corpora in a temp root: an absent corpus yields a coverage section and null indicators; a torn final line costs one record and no more; a `review-artifacts-*.json` file is counted `unrecognized` and reaches no indicator; a receipt with unobserved remediation rounds leaves the denominator short rather than counting as first-pass; `diffMatchesPlan` is always `computable: false`; a window excludes records from values *and* denominators; the scan cap reports truncation; and the CLI exits 0 with every indicator null. Assert no file was created under the temp root after a run.

## 2. Skill and documentation

- [x] 2.1 Write `skills/report/SKILL.md` implementing the `report/non-gating` skill contract: obtain every figure from `interlock report --json` rather than deriving one, state what each figure does and does not license, name what would have to be recorded to make a thin indicator meaningful, and never recommend a change to a gate, cap or threshold on the basis of a figure. Satisfy the existing structural gate in `test/skills.test.mjs` — frontmatter key allowlist, `name` matching the directory, and the description + `when_to_use` length window under the 1 536-character listing cap.
- [x] 2.2 Add `docs/11-the-indicators.md`: each indicator, its source corpus, its denominator, the declared gap at `diffMatchesPlan`, and why the command gates nothing. Add its row to the `README.md` docs table.
- [x] 2.3 Amend `README.md:230` so its promise stays true and precise — `interlock report` reads all three corpora and still changes nothing about what the workflow does — without weakening the statement that no accumulated outcome relaxes a gate.

## 3. Verify

- [x] 3.1 Run `npm test` and `claude plugin validate . --strict`; both must pass. Run `interlock report` and `interlock report --json` against this repository and record the observed census in the change directory as `baseline.md`. Report the figures; do not tune anything from them.
