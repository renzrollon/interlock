# Decisions — add-interlock-report

| id | question | class | resolution | evidence |
|----|----------|-------|------------|----------|
| D1 | Which corpora does the report read — the outcome corpus alone, or all three? | human_resolved | All three, with every figure labelled by provenance. | human chose "All three, provenance-labelled" 2026-08-26. An outcomes-only reader prints an empty census here: `.claude/learning/outcomes.jsonl` does not exist |
| D2 | Is a rate ever emitted without its denominator? | agent_resolved | Never. Value travels with `observedOf` / `notObserved`; a zero denominator yields `null` plus a reason, never `0`. | `lib/doctor.mjs:26-31` "an unknown is not a pass"; `lib/outcomes.mjs:141-148` "a biased control group is worse than none, because it looks like evidence" |
| D3 | What does the report print first? | agent_resolved | Corpus coverage. Every later `null` is then self-explaining. | Census: 0 of 729 trajectories carry a `run-receipt`; 2 carry a `run-start` |
| D4 | How are `.claude/metrics/` files classified? | agent_resolved | By their `schema` key. Unrecognized shapes are counted as `unrecognized` by basename and excluded from every indicator. | `lib/metrics.mjs:91` writes `review-<change>-<stamp>.json`; `skills/review-artifacts/SKILL.md:104` writes `review-artifacts-<change>-<stamp>.json` with finding bodies — the glob `review-*.json` matches both |
| D5 | Is "does the merged diff match the plan" approximated from `planStatus` or from the replan rate? | agent_resolved | Neither. Emitted as `{ computable: false, reason, wouldRequire }`. | `planStatus` describes stored-plan reuse over planning inputs (`lib/plan-fingerprint.mjs:60-67`), not a diff comparison; restating one requirement as another is the defect the `add-interlock-evals` artifact review blocked on |
| D6 | Are replan rate and CLI-exit health in scope, given neither is in the playbook's list? | agent_resolved | Yes. They are the two indicators with dense data today and both answer a real question. | 150 of 1 785 `wave-action` events carry `source: "replan"`; 225 of 1 785 `cli-exit` events are non-zero |
| D7 | What is the exit status? | agent_resolved | Always 0, stated in the module header, the `USAGE` exit-code table, and a spec requirement, and asserted by the spine test. | `lib/drift.mjs:27-35` and `bin/interlock:1055-1057` are the report-never-blocks precedent; §4.15(a) forbids gating on these records |
| D8 | Is the scan bounded, and where does the bound live? | agent_resolved | Bounded, published through `interlock limits`, and truncation is reported rather than sampled silently. | 729 trajectory files today and no upper bound; `add-interlock-evals` D14 established that caps live in `lib/limits.mjs` and are cited, never restated |
| D9 | Which filters does the command accept? | agent_resolved | `--since <iso>` and `--change <name>` only; anything else comes from `--json`. | "Trending toward zero" needs a window; `change` is the corpus's own key |
| D10 | Does this ship a skill, and what may the skill do? | human_resolved | Yes — CLI, skill and a docs page. The skill explains a census and never recomputes a figure or converts one into a gate recommendation. | human selected all three surfaces 2026-08-26; `skills/evals/SKILL.md`'s "obtain the verdict from the CLI rather than deriving it" is the same contract shape |
| D11 | Does the report write anything — a cache, an index? | agent_resolved | Nothing. Not even on a slow scan; the mitigation for slowness is `--since`, not a cache. | `lib/metrics.mjs:1-10` isolates the only write paths in `lib/` deliberately; a reader that writes joins that exception for no policy reason |
| D12 | Which route did this change take? | human_resolved | OpenSpec change first, then implement against it. | human chose "OpenSpec change first, then ship it" 2026-08-26, over direct implementation (the `lib/doctor.mjs` precedent) |
