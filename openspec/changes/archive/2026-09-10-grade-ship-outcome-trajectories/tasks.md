## 1. Process walker

- [x] 1.1 Add `evals/ship/trajectory.mjs`: a pure walker over run-log records that returns the three process criteria (`trajectory-required-event-types`, `trajectory-known-actions`, `trajectory-halt-on-unit-red`). Import `RUN_LOG_TYPES` and `ACTIONS`; do not copy those lists. A `run-complete` close requires `wave-action`, `cli-exit`, `verify-judgement`, and `run-receipt` in addition to whatever reconstructability already requires; a `run-halt` close does not fail solely for missing `verify-judgement` or `run-receipt`; any unknown `type` fails required-types; a present `action` that is missing from `ACTIONS` (including `"report"`) fails known-actions; `unitStatus` in `red` / `error` / `weakened` requires `run-halt` and forbids `run-complete`. Plant the failing and edge event lists under `evals/ship/sample/` and cover them offline in `test/spine` via `appendRunLogEvent` — no live ship, no network, no order match on implementer tools. Do not edit `lib/run.mjs`, `lib/run-log.mjs`, or `checkRunLog`.

## 2. Outcome grader and spoken coverage

- [x] 2.1 Compose the walker from `gradeArm` in `evals/ship/graders.mjs`: on the loop arm, load records with `interlock run-log show --json` and append the three process criteria after reconstructability; on the control arm, record them `n/a` beside ticks / trajectory / receipt. Keep `SHARED_CRITERIA` unchanged. Extend `ship-graders` (and `differenceFor` tests if needed) so a reconstructable planted log with an unknown `action` still passes `trajectory-reconstructable` / `run-log check` while known-actions fails, the control arm never fails the new ids, and the arm difference still carries no verdict and still excludes process criteria.

- [x] 2.2 Update `evals/ship/README.md` with the process criteria (JSONL events, not chat) while keeping the existing ACP coverage tokens. Pin the three new criterion-id tokens in `test/spine/ship-outcome-eval.test.mjs`. Update the Purpose of `openspec/specs/evals/outcome-run/spec.md` by hand so it names disk *and* run-log process. Do not add dependencies, lint, build, CI jobs, or wire any result into `ready` / `gate` / the ship loop.
