# Decisions — add-interlock-evals

| id | question | class | resolution | evidence |
|----|----------|-------|------------|----------|
| D1 | Seed the suite from `.claude/memory/failure-modes/` as requested, or from another corpus? | agent_resolved | Seed from `openspec/changes/archive/*/proposal.md` `## Why` sections and `CHANGELOG.md`. The requested source yields no usable cases. | All 9 files under `.claude/memory/` read and triaged: 0 eval-shaped, 4 unit-testable, 5 convention; 4 already have assertions in `test/workflows.test.mjs`. See brief §Codebase Findings |
| D2 | Where does the suite live, and does the manifest need an `experimental.evals` key? | agent_resolved | `evals/` at the repository root; no manifest key. Root is the plugin root, so this is the harness default. | `.claude-plugin/plugin.json:1-22` has no `experimental` key; harness default dir is `evals/` under the plugin root |
| D3 | Does the CI gate block on a score threshold on day one? | agent_resolved | No. Advisory only; promotion to blocking requires observed variance across more than one run. | No baseline scores exist in the repo; judged graders decide on a majority of 3 haiku votes with unmeasured variance. brief §Risks |
| D4 | How are fork-originated pull requests handled? | agent_resolved | Skip the job when head repo ≠ base repo, and never mark it a required check. | Bare `pull_request:` in `.github/workflows/ci.yml:3-7` fires on forks, where repository secrets resolve empty |
| D5 | One run shape or two? | agent_resolved | Two: a tagged deterministic `smoke` subset on same-repo PRs, and the full suite with ablation on a schedule. | 8 cases × 3 runs × 2 ablation arms = 48 agent runs, est. 1.2–2.5 h. brief §Options Considered |
| D6 | Which grader types should cases prefer? | agent_resolved | Deterministic graders wherever the assertion is pattern, tool-invocation, file-existence or ordering shaped; judged graders justified in writing. | The strongest seed case (handoff `status` ∈ `{ok,blocked,partial}`) is set-membership; judged graders cost 3 judge calls each |
| D7 | Is the `interlock:evals` skill in this change or a follow-up? | agent_resolved | In this change. Authoring a case from a failure and triaging a run are judgement work the harness does not cover. | `openspec/changes/add-interlock-evals/specs/evals/authoring/spec.md`; the seeding procedure in this change is what the skill encodes |
| D8 | Where does the early-access enablement variable live? | agent_resolved | The CI job's `env:` block and contributor prerequisites — never committed repo settings. | Binary states a repo `.claude/settings.json` / `settings.local.json` `env` value normally leaves the command gated off. brief §Codebase Findings |
| D9 | How does the new workflow file avoid being silently untracked? | agent_resolved | Force-add it, and verify with `git ls-files .github/workflows/`. | `.gitignore:30-34` ignores `.github/workflows/`; `git ls-files .github/` returns only `ci.yml` |
| D10 | Does the CI run publish its HTML report? | agent_resolved | No. Publication disabled; report retained as a build artifact. | Reports publish to claude.ai by default when the account supports artifacts; an unattended job has nobody present to consent |
| D11 | Which case file form do fixture-bearing cases use? | agent_resolved | `case.yaml` with `context.scaffold_script`, and the runner sets the scaffold flag; non-fixture cases stay on `prompt.md`. | `prompt.md` frontmatter cannot express `context.*`; scaffold is off by default. `design.md` §Risks D11 |
| D12 | What cost ceiling should the metered runs carry, and should model access be provisioned in CI at all? | agent_resolved | Neither answer needs to block. The job skips cleanly with no credential, so the change lands and runs offline and provisioning becomes a later config action. Starting ceilings $2 (PR smoke) and $15 (scheduled full run), tuned from observed `cost_usd`. | human delegated the decision 2026-08-26 ("can you decide automatically"); `design.md` D15 |
| D13 | Does the regression / variance / no-signal classification live in the skill or in the CLI? | agent_resolved | The CLI. `interlock evals triage` owns the verdict via exit status; `lib/evals-triage.mjs` holds the rules; the skill explains but never reclassifies. | Artifact review blocker, `.claude/metrics/review-artifacts-add-interlock-evals-20260826-115640.json`; `interlock gate` is the existing exit-code-as-verdict precedent; `design.md` D13 |
| D14 | Where do the new caps (cost ceilings, runs, threshold) live? | agent_resolved | Published through `interlock limits` alongside the existing twelve, not restated in workflow YAML. | `interlock limits` output; README "Every cap the loop obeys, so nothing restates one"; `design.md` D14 |
| D15 | How are the case-suite requirements enforced, given they are statically checkable? | agent_resolved | `test/evals.test.mjs`, a structural gate in the shape of `test/skills.test.mjs`; runs offline in the existing npm test job so it covers fork PRs too. | Artifact review blocker (same findings file); `test/skills.test.mjs` is the existing pattern; `design.md` D15 |
