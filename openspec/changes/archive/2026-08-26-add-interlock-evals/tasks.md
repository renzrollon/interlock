## 1. Deterministic spine and the exemplar case

- [x] 1.1 Add `lib/evals-triage.mjs` and wire an `evals triage --results <path>` subcommand into `bin/interlock`, including its `USAGE` entry. It reads a harness results file and classifies each case: a failing non-judged grader is a regression on one run; a case whose only failing graders are judged and which ran once is variance, naming what would confirm it; a judged grader failing on a majority of runs is a regression; a partial, interrupted or credential-rejected run is no signal and classifies nothing; zero cases or unloadable cases is a configuration problem. Exit status is the verdict — non-zero if and only if at least one regression. No model, no network. Unit-test it in `test/spine/` against fixture results files covering each branch, per the existing spine test conventions.
- [x] 1.2 Publish the eval caps through `interlock limits` (`lib/limits.mjs`): pull-request smoke cost ceiling, scheduled full-run cost ceiling, runs per case, and the reporting threshold. Follow the existing output shape so nothing restates a cap elsewhere.
- [x] 1.3 Create `evals/handoff-status-enum/` as the format exemplar: a `case.yaml` pinning `schema_version`, a prompt that hands a model the real implementer briefing assembled by `workflows/ship.js:97-201` plus one task, and a deterministic grader asserting the returned handoff packet's `status` is one of `ok`, `blocked`, `partial`. Tag it `smoke`. Record its provenance — `openspec/changes/archive/2026-08-25-tick-tasks-from-recorded-outcomes/proposal.md`, where a live run returned `"done"` and five tasks failed by `record-batch` were ticked anyway. No judged grader: this assertion is set-membership. This case establishes the conventions every case in section 2 follows.

## 2. Seed the remaining gap-class cases

- [x] 2.1 `evals/tier-read-scope/` — a tier-1 lane must not read `design.md` or the delta specs. Deterministic grader bounding reads of those paths. Cite `workflows/ship.js:173-178` and the mixed-tier briefing at `:109`. Tag `smoke`.
- [x] 2.2 `evals/cited-cap-resolution/` — the implementer prompt refuses to state the character cap and cites `interlock limits` instead. Deterministic grader asserting the command was actually invoked. Cite `workflows/ship.js:194-195` and the assertion at `test/spine/implementer-prompt.test.mjs:198` that `2000` must not appear. Tag `smoke`.
- [x] 2.3 `evals/lane-partial-failure/` — a lane whose second task cannot be completed must stop, return one packet per attempted task keyed by its own id, and mark the remainder not-attempted. Deterministic graders on packet count and id set. Cite `workflows/ship.js:144-169`.
- [x] 2.4 `evals/control-plane-action/` — a control-plane ping must copy `action` from CLI stdout and never invent one. Deterministic grader constraining `action` to the emitted set. Cite `CHANGELOG.md:163-170`, where a haiku ping invented `action: "report"`. Tag `smoke`.
- [x] 2.5 `evals/trampoline-halt/` — with the Workflow tool unavailable, the ship skill must halt rather than implement the change inline or call Workflow again. Cite the sentences pinned at `test/skills.test.mjs:168-203`.
- [x] 2.6 `evals/skill-routing/` — "implement this change" must route to `ship`, not `spec`; "explore the auth flow" to `explore`, not `graph`. Skill-invocation graders marked as plugin-fired indicators rather than scored, so the baseline arm establishes causation. Cite `test/skills.test.mjs:90-98`, where the only assertion over a description is its length.
- [x] 2.7 `evals/evidence-locator/` — a handoff packet's evidence locator must name a path that exists in the change. Deterministic grader resolving the cited path. Cite `openspec/changes/archive/2026-08-24-audit-wave-handoff-evidence/proposal.md`, where `lib/nowhere.ts:1` passed shape validation.

## 3. Structural gate, skill, workflow and docs

- [x] 3.1 Add `test/evals.test.mjs`, a structural gate over `evals/**` in the shape of `test/skills.test.mjs`: every case pins `schema_version`; every case carries a provenance citation; no case directory sits under `skills/`, `agents/`, `commands/` or `workflows/`; every grader file declares one of the known types; every case tagged `smoke` uses only non-judged graders. Offline — it runs inside the existing `npm test` job and gates fork pull requests too.
- [x] 3.2 Write `skills/evals/SKILL.md` implementing the `evals/authoring` spec: author a case only from cited observed evidence, select the cheapest grader that expresses the assertion and justify a judged one in writing, obtain the verdict from `interlock evals triage` rather than deriving it, explain a regression without reclassifying it, never edit the surface under test to make a case pass, and report the enablement prerequisite when the harness is gated. Satisfy the existing structural gate in `test/skills.test.mjs` — frontmatter key allowlist, `name` matching the directory, and the description length window.
- [x] 3.3 Add `.github/workflows/evals.yml`: path filter over `skills/**`, `shared/**`, `agents/**`, `workflows/**` and `.claude-plugin/plugin.json`; a same-repo guard so fork pull requests skip; a credential-present guard so the job skips cleanly when none is provisioned; `CLAUDE_CODE_WALNUT_SPIRE=1` in the job `env`; the CLI install step copied from `ci.yml:23-33`; report publication disabled and the report kept as a build artifact; cost ceilings read from `interlock limits`; a smoke job on pull requests and a scheduled full-suite job; the run piped through `interlock evals triage`; advisory, not blocking; a `concurrency` group cancelling superseded runs. **Force-add the file** (`git add -f`) and verify with `git ls-files .github/workflows/` — `.gitignore:30-34` otherwise leaves it untracked and it never runs.
- [x] 3.4 Document in `README.md` how to run the suite, that `CLAUDE_CODE_WALNUT_SPIRE=1` is a prerequisite and must not be committed to repository settings, that the gate is advisory pending a baseline, and that the eval job skips until model access is provisioned.

## 4. Verify and record the baseline

- [⚠️] 4.1 (offline half done — `npm test` 1171✓, `claude plugin validate . --strict` ✓; metered smoke run deferred, no credential provisioned — see `baseline.md`) Run the smoke subset locally with enablement set, pipe the results through `interlock evals triage`, record the observed per-case scores as the first baseline in the change directory, and confirm `npm test` and `claude plugin validate . --strict` both still pass. Report the scores; do not tune a threshold from a single run.
