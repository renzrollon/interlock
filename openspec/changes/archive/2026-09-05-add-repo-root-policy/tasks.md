## 1. Root policy files and docs

- [x] 1.1 Write `CLAUDE.md` at the repo root, under one page. Sections: **Commands** (`npm test` — note it is `node --test` over a `find`-piped file list, and that a bare `node --test 'test/**'` glob collects zero tests on Node 18/20, per `.claude/metrics/fix-tests-20260812-000000.json`; `npm run validate`), **Conventions** (zero runtime dependencies — stdlib Node only, `type: module`, `.mjs` throughout, Node >= 18), **Architecture** (`bin/` = executables on a user's PATH, `lib/` = pure decision modules, `skills/` = model-facing prose, `hooks/` = PreToolUse/SessionStart guards, `workflows/` = the ship orchestrator, `openspec/` is OpenSpec's and is written through its CLI), **Things to get right** (gate thresholds live in the CLI so a model cannot re-argue them — see `docs/12`; guards fail open, never closed; a corpus line lost is never fatal). Include a **Verifying your work** block naming `npm test` and stating that a failing test is fixed in the code, never by editing or deleting the test. Add one line declaring this file canonical over `AGENTS.md` (design D1).
- [x] 1.2 Write `REVIEW.md` at the repo root per design D5. `## Owner`, `## What "Important" means here` (behaviour breakage, data leaks, policy breaches — phrased without restating a numeric threshold, per `docs/12`), `## Exclusions rationale`, and `## Do Not Report` listing exactly `test/fixtures/` and `test/graph/fixtures/`. In the exclusions rationale, record which command the path list binds: `readReviewPolicy` is called only from `interlock review` (`bin/interlock:1493`) and `interlock review-policy` (`bin/interlock:1527`), so the list governs `review-code` and is inert for `review-artifacts`, which reaches its verdict through `interlock gate`. Verify with `interlock review-policy --json` — it must parse with no reported problems and return both the prose and the two paths.
- [x] 1.3 Add a corpus-persistence section to `docs/11-the-indicators.md`, attached to the existing three-corpus table (design D2). State the rule: a repo running `/interlock:ship` on its own product commits `.claude/ship/`, `.claude/learning/` and `.claude/metrics/` as its audit trail; a repo developing the harness itself excludes them. Give the recommended `.gitignore` block for each posture. Record two honest caveats: `.claude/learning/outcomes.jsonl` is a single append-only file and so a merge-conflict site when two branches ship in parallel, whereas per-run and per-review files are conflict-free by being separate paths; and this repository takes the exclude posture, with `.claude/ship/runs.polluted-2026-08-29/` as the evidence for why.
- [x] 1.4 Correct the two stale assertions in `docs/10-agentic-workflow-ship-and-spec.md`. Line ~248 claims `.gitignore` covers graph/handoff/metrics/testing "but **not** `.claude/ship/` or `.claude/memory/`" — both are now listed (`.gitignore:19-29`), so both halves of that sentence are wrong. Line ~408 still lists "Gitignore `.claude/ship/`" as an open action item alongside an undecided `.claude/memory/`; both were decided. Replace with the current state and link to the new docs/11 section rather than restating the policy.

- [x] 1.5 Add one line to `README.md`'s install section pointing at the new docs/11 corpus-persistence section (design D3a). Bootstrap only runs on brownfield repos with code and no specs, so greenfield adopters and repos that already have specs never see its recommendation; the README is the one surface every adopter passes regardless of entry path.
- [x] 1.6 Make this repo's `.gitignore` match the posture it now documents. Found during 2.1 by running the instruction against this repo: `git check-ignore -q` returns 0 for `.claude/ship/` and `.claude/metrics/` but **1 for `.claude/learning/`** — the outcome corpus was never excluded. This is the split state the new bootstrap step exists to surface, in the repo that wrote it, and it makes docs/11's claim that "this repository takes that side" false until fixed. Add `.claude/learning/` to the `.claude/` runtime-state block. Verify all three return 0.

## 2. Bootstrap reporting step

- [x] 2.1 Add the corpus-persistence step to `## 6. REPORT` in `skills/bootstrap/SKILL.md` (the closing report section, currently at `:160`), implementing all three requirements of `specs/bootstrap/corpus-persistence`. **This task also widens the skill's `allowed-tools` frontmatter (`:7`) to include `Bash(git check-ignore *)`** — it is one task and not two because both edits touch the same file and could not run as siblings in one wave. The widening is not optional bookkeeping: bootstrap's grant is currently `Bash(openspec *) Bash(interlock-graph *) Agent Read Write Glob Grep` with no `git` of any form, and because a failed check routes to the undetermined branch, shipping the instruction without the permission makes the step report `undetermined` on every repo forever while appearing to work. Keep the grant narrowed to `check-ignore`; do not grant unrestricted `git`. Then: use `git check-ignore -q <path>` once per corpus path and read the exit status — do not parse `.gitignore` (design D3). Report each of the three paths separately, never collapsed into one verdict. State the recommendation as conditional on whether the repo will run `ship` on its own product, never as a verdict about the repo. Route a non-zero exit (unreadable `.gitignore`, or not a git repository) to a "posture undetermined" branch that says why and lets bootstrap complete normally. Name the entries a person could add; never create, append to, or edit `.gitignore`. Link to the docs/11 section from 1.3 for the rationale.

## 3. Pin the instruction and the files

- [x] 3.1 Add assertions to `test/skills.test.mjs` covering all three requirements of `specs/bootstrap/corpus-persistence` — following the `--metrics` precedent in `docs/11`, where a prose instruction nobody asserted silently stopped running for a year (the existing pin is at `test/skills.test.mjs:205`). Assert: the three corpus paths and `git check-ignore` appear in the bootstrap skill; its `allowed-tools` admits `git check-ignore` and does **not** grant unrestricted `git`; and the file carries no instruction to write, append to, or create `.gitignore`. Do **not** match an exact sentence — design D4 notes a brittle string match gets deleted at the first reword, which removes the only mitigation the step has.
- [x] 3.2 Assert the root policy files themselves, in `test/spine/review-policy.test.mjs` or a sibling: `CLAUDE.md` and `REVIEW.md` exist at the repo root and are non-empty, and `readReviewPolicy(repoRoot)` on the **real** root returns a non-empty `excludePaths`. The existing tests write policy fixtures into temp dirs (`:25`) and never touch the real root, so today a deleted `REVIEW.md` keeps the whole suite green while silently restoring the empty policy this change exists to end — the change's own premise, recurring undetected. Run `npm test` and confirm the full suite is green.
