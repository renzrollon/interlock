## 1. Telemetry fidelity and the HTML renderer

- [x] 1.1 Pin a temporary root for every CLI test that spawns the real binary. `test/spine/cli.test.mjs:26`'s `run()` helper passes neither `cwd` nor `--root`, so the CLI's default root resolves to the repository and each `wave-state create` writes into the live `.claude/ship/runs/`. Give the helper the per-test temp dir it already creates (`dir`), audit `test/spine/host.test.mjs` and every other `spawnSync([BIN, …])` site in `test/` for the same shape, and add a guard test asserting that running the suite creates no file under the repository's own `.claude/ship/`. Reproduce first: `ls .claude/ship/runs/*.jsonl | wc -l`, run `node --test test/spine/cli.test.mjs`, count again — it is +11 today. Some tests may currently pass by reading the developer's accumulated corpus; a failure surfaced by this fix is a finding to report, not to paper over (design.md — Risks).
- [x] 1.2 Move the polluted trajectory corpus aside once, after 1.1 lands and the suite is green. `mv .claude/ship/runs .claude/ship/runs.polluted-<date>` rather than deleting — D8 chose reversible. 5690 of 5704 events are `change: "unnamed"`; the 14 real events from `add-interlock-evals` and `verify-receipt-scratch` go with them, which D8 accepts and records. Confirm afterwards that `interlock report` reads an empty trajectory corpus and still exits 0.
- [x] 1.3 Add `--metrics <change>` to `bin/interlock`'s `gate` case, mirroring the `review` case exactly: `die('--metrics requires a change name')` on a valueless flag, `writeReviewMetrics(root, {change, counts: result.counts})` after `evaluateGate`, `warn` on a failed write, the metrics line appended to the text output, and `metrics` merged into the `--json` object — while the verdict, the exit status and the pre-existing JSON fields stay untouched. Cover with tests for: a record written and recognized by the report's classifier, a passing gate still emitting zeroes, an unwritable root still returning its verdict and exit code, a valueless flag refused, and no flag writing nothing. Satisfies `review/metrics-emission` requirements 1–4.
- [x] 1.4 Add `lib/report-html.mjs` exporting a pure renderer from a `buildReport()` result to an HTML string — no I/O, no corpus reads, no indicator computation (D4). Coverage section first, then indicators (`report/html-surface`). Escape every interpolated value at one choke point: corpus file names and reason strings are untrusted input. Render unobserved and not-computable indicators at full weight with their reason, never as a zero or an empty cell (D5). Inline CSS and inline SVG only, for the gate-exit distribution and coverage proportions (D6); no threshold line, target marker, trend arrow, or colour encoding health. Test with: a fully populated report, a wholly empty one, a report whose metrics file name contains `<script>` (asserting inert output), and an assertion that the document contains no reference resolving to a host.
- [x] 1.5 Update `skills/review-code/SKILL.md:95` to pass `--metrics <change>` on its `interlock review` line and `skills/review-artifacts/SKILL.md:122` to pass it on its `interlock gate` line, then add the pinning assertions to `test/skills.test.mjs` (following the `test/skills.test.mjs:195` precedent) so either skill dropping the flag fails the suite. Satisfies `review/metrics-emission` requirement 5. Depends on 1.3 for the gate flag to exist.

## 2. Wiring and documentation

- [x] 2.1 Wire `--html [path]` into `bin/interlock`'s `report` case: render via `lib/report-html.mjs`, write to stdout when no path is given and to the named path otherwise (D9), refuse `--html` together with `--json` (D10), and write nothing on an unwritable destination rather than leaving a partial file. Exit stays 0 whenever a document was produced, including on empty corpora. Test the exit status on an empty corpus, the no-partial-file behaviour, the mutual exclusion, and cross-surface equality — one report object rendered to text, JSON and HTML carries the same value and denominator for every indicator. Depends on 1.4.
- [x] 2.2 Document both surfaces. `docs/11-the-indicators.md:24` currently names `interlock review --metrics` as the review-metrics writer without noting that no skill passed it; correct it to name both entry points and state that the skills now request emission. Add `interlock report --html` to `README.md` and to `skills/report/SKILL.md`, stating what the HTML surface is (a generated file, not a service) and that it issues no verdict. Depends on 1.3, 1.5 and 2.1 having landed their behaviour.
