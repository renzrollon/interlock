## 1. Cap readers and the tracked workflow file

Three independent files, none needing the others' output. All offline — no credential required.

- [x] 1.1 Assert the eval workflow definition is tracked (R4). Add a test to `test/evals.test.mjs` — the offline structural gate over the eval surface, and so the right home — that spawns `git ls-files .github/workflows/evals.yml` with `node:child_process` and asserts non-empty output, failing with a message that names the file and says `.gitignore:38` ignores its directory so the file must be force-added. Guard on `git rev-parse --is-inside-work-tree`: where that fails (a published tarball, an export with no `.git`), skip with the reason stated rather than passing — design D6. While in the file, correct the stale `.gitignore:37` reference in the header comment of `.github/workflows/evals.yml:13` to `:38`. Satisfies `evals/gate` — "The eval workflow definition is tracked in version control".

- [x] 1.2 Make the scheduled full-suite job read runs-per-case from the published limits (R3, first half). In `.github/workflows/evals.yml`, extend the `full` job's `Resolve cost ceiling` step (`:139-145`) to also emit `evals.runsPerCase` as a step output, reading it through the same `interlock limits --json` pipe the ceiling already uses — no second mechanism, and no number written into the YAML. Pass it as `--runs "${{ steps.caps.outputs.runs }}"` on the `Run full eval suite` step (`:146-156`), and update that step's comment, which currently says runs defaults to each case's own value. Leave the `smoke` job's `--runs 1` alone: it is the single-run posture of the per-change subset, not a restatement of the cap (design D3). Satisfies `evals/gate` — "The scheduled full-suite run reads its runs-per-case from the published limits".

- [x] 1.3 Remove the cap that governs nothing (R3, second half). Delete `reportingThreshold` from `EVAL_CAPS` (`lib/limits.mjs:157-162`) and its row from `formatLimits()`'s `evalRows` (`:245`) in the same edit — object and printed surface together, following the `memoryEntriesPerRun` precedent at `:74-79`. `lib/evals-triage.mjs` branches on each grader's `passed` boolean and never reads a score, so there is no path the threshold could govern without inventing one (design D4). Rewrite the `EVAL_CAPS` doc comment (`:131-146`): the paragraph justifying its exemption from the reader-invariant test stops being true in task 2.1, and the sentence describing the reporting threshold goes with the cap. Grep the token first to confirm the blast radius — its only live sites are this module and its printed row; the mention in `openspec/changes/archive/2026-08-26-add-interlock-evals/baseline.md:22` is an accurate historical record of what the CLI published on that date and is not edited.

## 2. The extended reader invariant

Needs 1.2 and 1.3 to have landed: the invariant fails while `reportingThreshold` still exists and while `runsPerCase` has no reader.

- [x] 2.1 Extend the cap-authority invariant to `EVAL_CAPS` and to the CI directory (R3, third half). Rework `test/spine/limits.test.mjs:106-128` to walk `.github/workflows/` alongside `lib/`, `bin/` and `workflows/`, and to check both cap groups: for `LIMITS` the reader token stays `LIMITS.<cap>`; for `EVAL_CAPS` a reader counts when it names either `EVAL_CAPS.<cap>` (a `lib`/`bin`/`workflows` reader) or `evals.<cap>` (a CI reader, which reads the field name the CLI publishes at `bin/interlock:1429`, not the export name). The bare group name must never count — `bin/interlock:1429` forwards `evals: EVAL_CAPS` to the printed surface and a matcher that accepted it would mark every eval cap read, restoring exactly the blindness being removed (design D5). Keep the existing exclusion of `lib/limits.mjs` itself, and keep the failure message naming each unread cap. Add a removal assertion for `reportingThreshold` beside the existing `memoryEntriesPerRun` one at `:156-162`, asserting it is absent from `EVAL_CAPS` and that `formatLimits()` no longer prints its row. `REPORT_CAPS` and `EFFORT` stay outside this check — widening it further is a separate judgement (design, Open Questions). Verify with `npm test` and paste the output. Satisfies `ship/cap-authority` — "Every advertised cap SHALL be enforced by code".

## 3. The first metered run

**Human prerequisite — an agent cannot complete this task.** It needs a provisioned model credential (`ANTHROPIC_API_KEY`) and the early-access enablement variable (`CLAUDE_CODE_WALNUT_SPIRE=1`), neither of which any part of this repository can supply and neither of which may be committed (`evals/gate` — "Enablement is supplied by the environment, not committed to the repository"). Without them, stop here and say so: do not synthesise a results file to unblock section 4 (design D1).

- [x] 3.1 Run the smoke subset once and record the observed baseline (R1). Use the command already written into `openspec/changes/archive/2026-08-26-add-interlock-evals/baseline.md:26-32` — `--tag smoke --runs 1 --ablation none --no-publish`, ceiling resolved from `interlock limits --json`, results written to a JSON file — then pipe the results through `interlock evals triage --results <file>` and record its verdict. Replace that file's `Status: DEFERRED` header and its empty `### Observed scores` section with the per-case score for each of the four smoke cases, the `cost_usd` the run reported, and the exact parameters used. Do not tune any threshold from this single run: the gate stays advisory (archived design D3). If the run comes back partial or as a configuration verdict, record that outcome exactly as it happened — the cases that ran carry scores, the rest carry none, and the record says the run was partial. Keep the results JSON file; section 4 is its only consumer. Satisfies `evals/gate` — "The first metered run is recorded as an observed baseline".

## 4. The results fixture

Needs 3.1's output file. Cannot start earlier and must not start with a stand-in.

- [x] 4.1 Commit the run's results file as a fixture (R2, first half). Copy the JSON file 3.1 produced to `test/fixtures/evals/` — deliberately not under `evals/`, which is the harness's discovery root and is walked by `test/evals.test.mjs`'s structural gate (design D7). Redact by removal only: delete any credential, environment dump, transcript body or prompt text; keep every field triage reads byte-for-byte (`cases[].id`, `cases[].loaded`, `cases[].runs[].graders[].name`/`type`/`passed`, and the top-level `partial`/`complete`/`reason` where present). Never rewrite a retained value — editing one forges the evidence the fixture exists to be. Add a sibling note stating which run produced it, what was removed, and which triage branches it does **not** pin: the smoke subset is four deterministic cases at one run each, so the judged branch, the indicator branch, the `loaded: false` branch and the partial-run branch stay unpinned and hand-built. Satisfies `evals/triage` — the "committed file exercises only part of the schema" scenario.

## 5. The fixture-backed triage test

Needs 4.1's committed file.

- [ ] ⚠️ 5.1 BLOCKED ON RECORDED DEFECT — do not force green. The 2026-09-04 run (task 3.1) exposed that `interlock evals triage` disagrees with the run's own report: the run recorded `tier-read-scope` as a failure (3/4), triage returned `pass` (4/4). Per `evals/triage` ("Triage disagrees with the run's own report") and design D7, the disagreement is **recorded as a defect and the assertion is not adjusted to match the contradicting behaviour** — so no fixture-backed test asserting triage's current output may be written, and this box stays unchecked. Root cause (see `test/fixtures/evals/README.md` and `baseline.md`): `lib/evals-triage.mjs` reads `cases[].id`/`cases[].runs[]` while `schemaVersion: 1` writes `cases[].name`/`cases[].arms.with[]`. Fixing triage's schema reading is out of this slice's scope (design Non-Goals; carried into Slice E2). Once that fix lands, this test lands with it, asserting the run's true verdict against the committed fixture.
- [ ] 5.1 Triage the committed fixture in a test (R2, second half). Add one test to `test/spine/evals-triage.test.mjs` that reads the fixture from disk with `node:fs`, passes the parsed object to `triage()`, and asserts the verdict, exit code and per-case classifications that the run in 3.1 actually produced — the first of the file's thirteen tests whose input is not hand-built (`:13-15` builds the other twelve). Assert on the fields, not only the verdict, so a renamed harness field fails rather than silently changing a classification. If triage's computed verdict contradicts what the run itself reported, record it as a defect and stop: do not adjust the assertion to match the contradicting behaviour (`evals/triage` — "Triage disagrees with the run's own report"). Verify with `npm test` and paste the output. Satisfies `evals/triage` — "Triage's reading of the results schema is pinned by a real results file".
