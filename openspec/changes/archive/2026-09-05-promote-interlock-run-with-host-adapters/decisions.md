# Decisions — promote-interlock-run-with-host-adapters

| id | question | class | resolution | evidence |
|----|----------|-------|------------|----------|
| P1 | Should the runner offer an API-key or billing switch for unattended runs if Anthropic's paused credit change returns? | agent_resolved | No switch in this change: the runner passes the environment through, names the billing path in a banner and the receipt, and points at the one docs paragraph to update. Choosing credentials is the operator's. | `.claude/handoff/explore-creating-or-reusing-harness-20260904-171500.md` §Pending Clarifications 1 and §Assumptions Made; memory `claude-subscription-programmatic-use-billing`; design.md D8, D10 |
| P2 | Should Codex or Qwen be reachable from `/interlock:ship` inside a Claude Code session? | agent_resolved | No. `/interlock:ship` stays on the Workflow runtime; the runner refuses the Workflow host id; other vendors are reached only through `interlock-run`. | `openspec/specs/workflow-host/spec.md:23-25`; explore brief §Pending Clarifications 2; design.md D10 |
| P4 | Is a Node ≥ 22 floor acceptable to adopt `acpx` or a vendor SDK? | agent_resolved | No; adapters shell out to CLIs and the package stays dependency-free on Node ≥ 18. | `CLAUDE.md` Conventions; design.md Non-Goals |
| D3 | What permission mode does the `claude` adapter run under, and do plugin hooks still guard under it? | agent_resolved | Default `bypassPermissions`, kept only if task 2.1's verification shows a PreToolUse deny still blocks; otherwise `dontAsk` plus `--allowedTools`. The finding is recorded in design.md Risks either way. | `hooks/guard-tests.mjs:1-17`; explore brief §Risks / Gaps ("Unverified: that plugin-provided hooks fire under claude -p --agent"); design.md D3 |
| D5 | Which flag selects the model on Qwen Code? | agent_resolved | Verified against the installed `qwen --help` by task 2.3 and encoded in the fixture; if unverifiable, no model flag is passed and the design says so. | explore brief §External Findings (Qwen Code); design.md D5 |
| D6 | One model map for every host, or one variable per host? | agent_resolved | One `INTERLOCK_MODEL_MAP` keyed by host, with `INTERLOCK_ACP_MODEL_MAP` kept as an alias for the `acp` entry. | `close-acp-model-routing` design D2; design.md D6 |
| D7 | Runtime-owned or driver-owned worktrees on the runner? | agent_resolved | Driver-owned for every runner host, at the path the CLI emits, folded by `run record-batch`. | `docs/08-harness-landscape.md:785-797`, `openspec/specs/ship/lane-merge/spec.md`; design.md D7 |
| D11 | Delete the old binary name or keep a shim? | agent_resolved | A shim for one release, removed in the next minor version. | `package.json` `bin`, `docs/04-when-it-stops.md:86`; design.md D11 |
