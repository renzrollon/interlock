## 1. Registry, rename and the manifest

- [x] 1.1 Create `lib/host/registry.mjs` exporting `HOSTS` (ids `acp`, `claude`, `codex`, `qwen`, each `{ id, create(opts), capabilities }` per design D1) and `createHost(id, opts)` that throws on an unknown id; create `lib/host/model-map.mjs` exporting `parseModelMap(env)` for `INTERLOCK_MODEL_MAP` (with `INTERLOCK_ACP_MODEL_MAP` folded in as the `acp` entry) and `resolveModel(hostId, slug, map, capabilities)` returning `{ value, applied, reason }` per design D6; move the ACP adapter's `parseModelMap`/`pickModelValue` use onto it.
- [x] 1.2 Rename `bin/interlock-ship-acp` to `bin/interlock-run`: add `--host <id>` / `INTERLOCK_RUN_HOST`, refuse an unknown id and `workflow` before any step with a message naming the known ids, create the host through the registry, pass `--host <id> --host-capabilities <json>` to `run start`, and rewrite the usage text; create the shim `bin/interlock-ship-acp` that prints the deprecation line to stderr and execs `interlock-run --host acp` with the same arguments (design D11); update `package.json` `bin` to add `interlock-run` and keep the shim; update `test/workflows.test.mjs`'s import allowlist to the new file name and to admit `lib/host/registry.mjs`, `lib/host/model-map.mjs` and the adapters.
- [x] 1.3 In `lib/run.mjs`: store `host` and its capabilities on the manifest at `run start`; in `decorate`, emit `isolation: 'worktree'` only when the manifest's `worktree` is `runtime`, and otherwise emit `worktree: { path }` per lane (path from the shared derivation) plus `mergeBase` on isolated batches (design D1, D7); in `run close`, sum per-spawn `usage` from the results file into per-wave and per-run output tokens, recording `unknown` when any spawn in a wave lacked it (design D9), and record `host: { id, billing, hooks, usage }` in `lib/receipt.mjs` (design D8).
- [x] 1.4 Create `test/spine/host-registry.test.mjs`: every adapter declares every capability key with an allowed value; ids are unique; `createHost` throws on unknown; extend `test/spine/run.test.mjs` with worktree emission by capability, usage summation with an `unknown` wave, and the receipt's host block.

## 2. Adapters and their fixtures

- [x] 2.1 Create `lib/host/claude-cli.mjs` implementing design D3 (`claude -p --output-format json --json-schema … --agent … --model … --permission-mode … --allowedTools …`, prompt on stdin, cwd from the spawn's worktree, `structured_output` as the result, `usage` attached, null on non-zero exit or missing output, `model-routing` events) and `test/fixtures/hosts/fake-claude.mjs` emulating the envelope; before keeping `bypassPermissions` as the default, verify with an installed Claude Code (`claude -p --permission-mode bypassPermissions --debug` and a PreToolUse hook that denies) whether a plugin hook deny still blocks, record the finding under Risks in this change's `design.md`, and switch the default to `dontAsk` plus `--allowedTools` if it does not.
- [x] 2.2 Create `lib/host/codex.mjs` implementing design D4 (`codex exec --json --output-schema <tmp> -o <tmp> -C <cwd> --ask-for-approval never --sandbox workspace-write [--model …]`, prompt on stdin, result from the `-o` file, usage scanned from the JSONL, unrouted when unmapped, `CHATGPT PLAN PATH` condition on the absence of `CODEX_API_KEY` and `OPENAI_API_KEY`) and `test/fixtures/hosts/fake-codex.mjs`; verify each flag against the installed `codex --help` if present and encode the verified spelling in the fixture.
- [x] 2.3 Create `lib/host/qwen.mjs` implementing design D5 (`qwen -p - --output-format json --json-schema … --yolo [model flag]`, prompt on stdin, validated payload from the envelope, unrouted when unmapped) and `test/fixtures/hosts/fake-qwen.mjs`; verify the model flag's spelling against the installed `qwen --help` if present and encode it, or record in `design.md` that it could not be verified and the adapter passes no model flag.
- [x] 2.4 In `lib/host/acp.mjs`, accept a per-spawn `cwd` from the worktree (design D7) and read the model map through `lib/host/model-map.mjs`; keep the negotiation from `close-acp-model-routing`.
- [x] 2.5 Create `test/spine/host-adapters.test.mjs` driving each adapter against its fixture: the exact argv (pinned), the prompt arrives on stdin, a schema is passed, the result equals the fixture's structured payload, non-zero exit yields `null`, usage is attached when present and absent otherwise, unmapped Codex/Qwen spawns report `applied: false` with `no mapping for <slug>`, Claude passes the slug through, a malformed `INTERLOCK_MODEL_MAP` fails host creation, and live tests run only when `INTERLOCK_LIVE_HOSTS` names the host (design D12).

## 3. Driver-owned worktrees and banners

- [x] 3.1 In `bin/interlock-run`: for each isolated batch, `git worktree add --detach <path> <mergeBase>` per lane before spawning, spawn with the worktree as cwd, remove folded worktrees after `run record-batch` and leave survivors; collect `model-routing` and adapter events; hand `run close` the host banners — `RUNNER HOST: <id> (experimental)`, `SUBSCRIPTION PATH: programmatic (<host>) …` for `claude` and for `acp` when `INTERLOCK_ACP_COMMAND` is the Claude binary, `CHATGPT PLAN PATH …` for Codex without an API key, `HOOKS NOT IN FORCE (<host>)` when `hooks: false`, `MODEL ROUTING UNAVAILABLE (<host>)` with labels and reasons (design D6–D8); exit with the closing step's code.
- [x] 3.2 In `test/workflows.test.mjs`, retarget the driver-level tests to `bin/interlock-run`, pin the new banner strings verbatim, and add an end-to-end run with `--host claude` against `fake-claude` in a temp git repository under `--isolate-waves`: worktrees are created from the merge base, folded and removed; a fixture that writes the same file from two lanes halts naming the path and leaves both worktrees; the `SUBSCRIPTION PATH` banner prints for `claude` and not for `qwen`; `--host codex` without keys prints `CHATGPT PLAN PATH` and `HOOKS NOT IN FORCE (codex)`; the shim prints its deprecation line and forwards arguments; `--host workflow` and `--host gemini` exit non-zero before writing a manifest.

## 4. Docs, skills and memory

- [x] 4.1 Update `README.md` (Experimental → "The runner": the host table with capabilities and env vars, the model map, the billing paragraph and the default-host policy from design D10, the install line for `interlock-run`), `docs/04-when-it-stops.md` (rename the ACP banner section to the runner banners and add the new ones), `docs/06-why-it-works.md` §14 (portability: the host-specific part is an adapter), `docs/08-harness-landscape.md` ("The part that is genuinely a problem": the host port is finished and isolation is closed on the runner; note the billing constraint), `docs/10-agentic-workflow-ship-and-spec.md` (host table and layers), `CLAUDE.md` Architecture (`bin/interlock-run`, `lib/host/`), `skills/ship/SKILL.md` §3 (binary name; still never invoked) with its pin in `test/skills.test.mjs`, `.claude/memory/coupling/acp-driver-import-allowlist.md` (new file name and admitted modules), and add a `CHANGELOG.md` entry naming the rename, the shim's removal schedule and the new banners.

## 5. Validate

- [x] 5.1 Run `openspec validate promote-interlock-run-with-host-adapters --strict`, `interlock validate promote-interlock-run-with-host-adapters` and `interlock ledger promote-interlock-run-with-host-adapters`; fix anything reported.
- [x] 5.2 Run `npm test` and `npm run validate` and confirm both are green.
