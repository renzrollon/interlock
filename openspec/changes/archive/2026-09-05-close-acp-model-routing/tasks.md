## 1. Adapter: negotiate the model per session

- [x] 1.1 In `lib/host/acp.mjs`, add and export `pickModelValue(slug, option, map)` implementing design D2 (map first and only if advertised, then exact `value`, then case-insensitive substring of `value` or `name`, else `null` with a reason), and `parseModelMap(raw)` for `INTERLOCK_ACP_MODEL_MAP` (a JSON object of slug → value; `undefined`/empty → `{}`; malformed → throw). Export `ACP_MODEL_MAP_ENV`. In `createAcpHost`, parse the map once at startup so a malformed value fails host creation.
- [x] 1.2 In `promptOnce` (`lib/host/acp.mjs`), after `session/new`: find the model option in the result's `configOptions` (id `model` or `category: 'model'`), pick a value, and call `session/set_config_option`; on any JSON-RPC error fall back to `session/set_model` once; when no option is advertised, try `session/set_model` with the raw slug once (design D3). Emit one `{ type: 'model-routing', label, requested, applied, via, value, reason }` event per spawn that carried a model. Keep the `_meta['interlock/model']` hint (D6). Replace `modelRoutingSupported: false` with `modelRouting: 'negotiated'` on the host object and update the file comment that explains the banner.
- [x] 1.3 In `test/fixtures/acp/agent.mjs`, add modes: `--config-options` (advertise a `model` select with values `haiku`, `sonnet`, `opus` plus a display-name-only match such as `{ value: 'x-large', name: 'Claude Opus' }`, accept `session/set_config_option` and echo the applied value as `appliedModel` and the method as `appliedVia` in the payload), `--set-model` (advertise nothing, accept `session/set_model` only), and `--config-error` (advertise the option, return a JSON-RPC error to `set_config_option`, accept `set_model`). The default mode advertises nothing and rejects `set_model` with `-32601`, as today.
- [x] 1.4 In `test/spine/acp-host.test.mjs`, cover every scenario in the delta: model applied via config option and echoed by the agent; fallback to `set_model` on a config error; no option advertised → `applied: false` with reason `no model option advertised` and the prompt still runs; an unadvertised slug → not applied with reason `slug not among advertised values`; `INTERLOCK_ACP_MODEL_MAP` maps `sonnet` to `x-large` and the agent echoes it; a map naming a value the agent does not advertise → not applied with reason `mapped value not advertised`; a malformed map fails `createAcpHost`; `pickModelValue` table tests for exact, substring-on-value, substring-on-name, case, first-wins, and no-match. Retarget the existing `modelRoutingSupported` assertion to `modelRouting === 'negotiated'`.

## 2. Driver: aggregate events, make the banner conditional

- [x] 2.1 In `bin/interlock-ship-acp`, collect `model-routing` events from the host's `onEvent`; at the summary print `MODEL ROUTING UNAVAILABLE (ACP host)` (verbatim, unchanged) followed by one `— <label>: <reason>` line per unapplied spawn only when at least one spawn that requested a model has `applied: false`; otherwise print the note `model routing: applied on N/N spawns via ACP session config` (design D4). Under `--verbose` print each routing event as it happens. Remove the unconditional banner and any read of `modelRoutingSupported`.
- [x] 2.2 In `test/workflows.test.mjs`, keep the verbatim banner pin and add two driver-level tests that run `bin/interlock-ship-acp` against a fixture change with `INTERLOCK_ACP_COMMAND` set to the fixture agent: with `--config-options` the summary contains the applied note and not the banner; with the default fixture the summary contains the banner and a `no model option advertised` reason line. Assert the driver computes nothing about routing itself: the only source of `applied` in the driver is the event field.

## 3. Docs and changelog

- [x] 3.1 Rewrite the `ACP HOST (experimental)` / `MODEL ROUTING UNAVAILABLE (ACP host)` section of `docs/04-when-it-stops.md` so the second banner is described as conditional, lists the three reasons it can print, names `INTERLOCK_ACP_MODEL_MAP`, and drops the sentence that ACP has no per-prompt model selector. Update the "No per-tier model routing" bullet in `README.md`'s Experimental section to say routing is negotiated per session and when it is not. Add a `CHANGELOG.md` entry.

## 4. Validate

- [x] 4.1 Run `openspec validate close-acp-model-routing --strict`, `interlock validate close-acp-model-routing` and `interlock ledger close-acp-model-routing`; fix anything reported.
- [x] 4.2 Run `npm test` and `npm run validate` and confirm both are green.
