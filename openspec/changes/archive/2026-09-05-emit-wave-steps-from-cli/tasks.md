## 1. Prompt modules, schemas and the step cap

- [x] 1.1 Create `lib/prompts/implementer.mjs` by moving `assembleImplementerPrompt` out of `workflows/ship.js` (the `ASSEMBLE_IMPLEMENTER_PROMPT` block) unchanged, exporting it; retarget `test/spine/implementer-prompt.test.mjs` to import it directly instead of evaluating the script's source, and confirm every fixture under `test/fixtures/prompts/` passes byte for byte without being regenerated (design D2 keeps the text; only its home moves).
- [x] 1.2 Create `lib/prompts/planner.mjs` exporting `assemblePlannerPrompt({ change, classifiedPath })` in the ACP driver's form (the model writes `classified.json` and reports; it never runs the CLI — design D7), carrying the grouping rules, the `dependsOn` contract, the tier ladder and the mode paragraph verbatim from the two drivers; rewrite `test/spine/planner-prompt.test.mjs` to assert the single module's assembled text contains tiers 1–5, the haiku routing rule, the mode paragraph and no envelope number, and delete the cross-driver comparison.
- [x] 1.3 Create `lib/prompts/verify.mjs` (`assembleVerifyPrompt({ change, context, steps, runId, statePath })` for `inter-wave` and `final`, unified from `bin/interlock-ship-acp:724-738` and `workflows/ship.js:2220-2260` so the agent runs the planned steps, spills oversized output and reports results while the CLI judges), `lib/prompts/replan.mjs`, `lib/prompts/commit.mjs` (lean form; no autonomy line), and `lib/prompts/stage.mjs` exporting `publishStageLine(stage, change, index)` rendered from `lib/ship-stage.mjs`'s `stagePath`, `MARKER_FIELDS` and `STAGES` (design D8). Each module exports one pure assembler.
- [x] 1.4 Create `lib/prompts/schemas.mjs` holding `SINGLE_TASK_SCHEMA`, `LANE_SCHEMA`, `PLANNER_SCHEMA`, `VERIFY_RESULT_SCHEMA`, `REPLAN_SCHEMA` and `COMMIT_SCHEMA`, moved from `workflows/ship.js` (`LANE_DISPATCH` block and inline literals) and `bin/interlock-ship-acp`; and `lib/prompts/index.mjs` exporting a registry `PROMPTS` (name → assembler → sample inputs) that `test/spine/prompt-integrity.test.mjs` iterates so the coverage count equals the registry size and an unregistered assembler module under `lib/prompts/` fails the test by name.
- [x] 1.5 In `lib/limits.mjs`, add `LIMITS.maxRunSteps: 200`, a `formatLimits` row and its `limits --json` field; in `test/spine/limits.test.mjs` add `lib/run.mjs` as its reader in the cap-authority group and assert the value is printed (design D9).
- [x] 1.6 Create `test/spine/prompts.test.mjs` covering: each assembler is deterministic for the same input; the verify prompt names the planned steps and the spill command and never the suite log; the stage line renders the marker path from `lib/ship-stage.mjs` and matches the shape `test/spine/ship-stage.test.mjs` pins; the schemas table exports every name the run program references.

## 2. The run program

- [x] 2.1 Create `lib/run.mjs`: the manifest (`.claude/ship/run.json`, schema `interlock.run/1`: change, flags, host, startedAt, steps) with read/write helpers; the step record builder (`interlock.run-step/1` with `action`, `then.argv`, `spawns[]`, `mergeBase` and the pass-through wave-state fields — design D3); the briefing writer (`.claude/ship/briefings/<label>.md`, first line `<!-- interlock briefing v1 label=<label> sha256=<hex> -->`, returning `promptPath`, `promptSha256`, `prompt` — design D2); the step counter that returns a `halt` step naming `maxRunSteps` once exceeded (D9); and `decorate(state, manifest)` that turns a wave-state step into a run step: lane spawns with `laneLabel`/`laneModel`/`laneEffort` from `lib/waves.mjs`, `spawnPrefix` from `lib/host.mjs`, `isolation: 'worktree'` plus the lane's worktree path (moved from the script's derivation) when isolated, a captured `mergeBase` for isolated batches; verify steps as a verify spawn from `interlock verify plan` or a skipped marker with the CLI's reason; `done` as `verify-final`, `close` (apply-only) or `host-tail` (any tail flag — design D6); `replan` as a replan spawn.
- [x] 2.2 In `lib/run.mjs` and `bin/interlock`, add `interlock run start --change <n> [--apply-only] [--no-commit] [--skip-e2e] [--skip-coverage] [--isolate-waves] [--mode solo|waves] [--max-parallel N] [--continue] [--review] [--handoff] [--conformance] [--strict] [--host <id>] --json`: write the manifest (design D4); run validation (`lib/artifacts.mjs` / the `validate` path) and halt-step on failure; run the plan-reuse probe (`lib/plan-fingerprint.mjs`), and on reuse create the run state, append `run-start`, publish the `implement` stage line into the first batch's briefings and return the first decorated step; otherwise return a `classify` step whose one spawn is the planner briefing with `then: run classified`.
- [x] 2.3 Add `interlock run classified --classified <file> --json`: coverage check (`tasks coverage`), `waves` with the manifest's `maxParallel`/`mode`, `plan.json`, `wave-state create`, the `run-start` trajectory event with the manifest's `mode`/`strict`/`host`, and the first decorated step (design D7). Halt-step with the CLI's reason on any failure.
- [x] 2.4 Add `interlock run record-batch --results <file> --json` implementing design D5: fold ok lanes via `lib/merge-lanes.mjs` when isolated (moving `captureMergeBase`/`runMergeLanesStep` semantics into the CLI), adjudicate with the logic moved from the `RECORDED_VERDICT` block (`laneOutcomes`, `adjudicateBatches`, `recordedOutcomes`), `wave-state record-batch --write-state`, `tasks tick`, the `LANE STOPPED EARLY` / `TASK TICK FAILED` / claim-derived / override banners on the step, and the next decorated step; then `interlock run judge --context inter-wave|final --results <file> --json` (verify judge; inter-wave → `wave-state record-verify --write-state` → next step; final → `commit` step, `close` on `--no-commit`, or `halt` per the completion gate, with `VERIFICATION SKIPPED` and `E2E FAILED (non-blocking by policy)` banners), and `interlock run replan --results <file> --json` (revised → `wave-state replan`; else `next`). Every step-yielding call increments the manifest counter and appends its `cli-exit`/`wave-action`/`agent-spawn` trajectory events the way `wave-state` does today.
- [x] 2.5 Create `lib/receipt.mjs` by moving the `BUILD_RECEIPT` block out of `workflows/ship.js`, and add `interlock run close --results <file> [--halt <reason>] [--host-banners <file>] --json` (design D10): build the receipt, `outcomes append`, `run-halt`/`run-complete`, `run-log check`, and return `{ action, exitCode, summary, banners }` where `summary` is the human summary text moved from both drivers' `finish()`/summary code, including the `LEAN SHIP` line and the "No degradation banners" line.
- [x] 2.6 Add the `run` case and usage text to `bin/interlock`, and a sequence guard: the manifest records the last emitted `then.argv`, and a `run` subcommand called out of sequence exits `1` with a reason naming the expected continuation.
- [x] 2.7 Create `test/spine/run.test.mjs`: build a temp repo with a fixture change and a fake `git`-initialised tree, then drive `interlock run` end to end with canned results (start → classify → classified → run-batch → record-batch → verify → judge → done → verify-final → judge → commit → close), asserting: every emitted `then.argv` names a subcommand in `bin/interlock`'s dispatch table (read as `test/workflows.test.mjs` does); every spawn's briefing file exists and its first-line hash matches `promptSha256` and the text; the implementer briefing equals `assembleImplementerPrompt` for the lane; `--apply-only` yields `close` at `done`; `--no-commit` yields `close` after final judge; a tail flag yields `host-tail`; the cap yields `halt` naming `maxRunSteps`; an out-of-sequence call is rejected; `record-batch` ticks recorded-ok ids, does not tick or fold a failed lane, and carries `TASK TICK FAILED` when `tasks.md` is read-only; `close` returns exit `1` with a `run-halt` event on `--halt` and `0` with `run-complete` otherwise, and includes a `--host-banners` entry in the summary.

## 3. Drivers become interpreters

- [x] 3.1 In `workflows/ship.js`, replace sections 1–3, 6 and 8 and `closeRun`/`recordOutcome`/`appendReceipt` with the interpreter: a `cli(argv, results)` ping (`type: interlock:ping`, its tools, haiku when probed) that writes the results file, runs the argv and copies stdout; `spawnAll(step)` mapping `step.spawns` through `pipeline`/`agent` with the by-reference bootstrap (a constant `BOOTSTRAP(label, path, sha)`), the spawn's `label`, `model`, `effort`, `type`, `tools`, `isolation`, and the spawn's schema plus a required `briefing`; the acknowledgement check that turns a missing or mismatched `briefing` into a null result with reason `briefing not acknowledged` (design D2); the loop `while (step.then) { results = await spawnAll(step); step = await cli(step.then.argv, results) }`; `RUNAWAY_BACKSTOP` at the runtime's 1000-agent ceiling with a comment that it is not a policy cap (D9); printing `step.summary` and the `GOAL MET: interlock ship` line. Keep `parseInvocation` (passing its flags to `run start`), keep sections 4–5 and 7 inline behind `if (step.action === 'host-tail')` with a `// HOST_TAIL_SEAM` marker (D6), and delete the `ASSEMBLE_IMPLEMENTER_PROMPT`, `LANE_DISPATCH`, `LANE_EFFORT`, `RECORDED_VERDICT`, `STEP_SHAPE` and `BUILD_RECEIPT` blocks, the stage-marker literals, `NEXT_ACTIONS`, `nextSchema`, `publishStageLine`, `captureMergeBase`, `runMergeLanesStep`, `MAX_LOOP_STEPS` and every prompt string the run program now emits.
- [x] 3.2 In `bin/interlock-ship-acp`, replace the loop, `runVerification`, the planner/replan/verify/commit prompts, `laneLabel`/`laneModel` use, the four `load*` source evaluators, `recordOutcome`, `appendReceipt` and `finish` with the same interpreter over `host.spawn`/`host.mapPipeline`/`host.runCli` (results written by the driver, `prompt` passed inline, `briefing` ignored), `RUNAWAY_BACKSTOP`, `--host-banners` for the ACP banners at close, and `host-tail` → exit `2` with the existing refusal message. Keep the import allowlist (node builtins and `lib/host*` only) and `parseArgv`.
- [x] 3.3 In `test/helpers/ship-harness.mjs`, add a mode in which a ping whose prompt names an `interlock run` command executes that command (via `lib/host.mjs` `runCli`) against a temp repo the harness prepares, returns its stdout as `cliStdout`, and records the results file the ping was asked to write; capture briefing files by label so `runShip` still returns `{ prompts }` keyed by spawn label; add `bootstrap` to the harness's `new Function` parameter list if the script reads it as a global (see `.claude/memory/coupling/ship-runtime-globals-need-a-harness-parameter.md`).
- [x] 3.4 In `test/workflows.test.mjs`: add `run` to the subcommand checklist; add the no-policy sweep over both drivers with a published token list (tier definitions, `dependsOn` rules, `interlock.wave-handoff/1`, `remediationRounds`, `laneModel`, `laneEffort`, `assembleImplementerPrompt`, verify skip reasons, merge rules) and a named allowance for the `HOST_TAIL_SEAM` region; pin the bootstrap text against `test/fixtures/prompts/bootstrap.txt`; assert the Workflow script never calls a `run` subcommand other than `run start` by literal (every other call comes from `then.argv`); keep the verbatim banner pins and move any banner now produced by the CLI to a pin over `lib/run.mjs`/`lib/receipt.mjs`; assert `RUNAWAY_BACKSTOP` is not equal to `maxRunSteps` or to it plus a constant.
- [x] 3.5 Retarget the remaining pins: `test/spine/plugin-agents.test.mjs` (the script keeps only the ping literals; worker `type`/`tools` now arrive on the spawn and are asserted in `test/spine/run.test.mjs`); `test/spine/ship-stage.test.mjs` (drop the script-literal drift check; assert `lib/prompts/stage.mjs` renders from `lib/ship-stage.mjs`); `test/spine/host.test.mjs` (add an interpreter-through-fake-host run from `run start` to `close` using canned spawn results); `test/spine/acp-host.test.mjs` unchanged; delete `test/spine/planner-prompt.test.mjs`'s cross-driver assertions if any survive 1.2.

## 4. Docs, skills and memory

- [x] 4.1 Update `docs/06-why-it-works.md` (§2 table: the script is an interpreter of CLI-emitted steps; §5.4; §14 "portability" now says the host-specific part is an interpreter and a transport), `docs/10-agentic-workflow-ship-and-spec.md` (§4 step by step, §5 layers), `docs/04-when-it-stops.md` (banners unchanged; note that `host-tail` is internal), `README.md` ("Why this and not a folder of prompts", the Experimental ACP bullets keep "Lean only"), `CLAUDE.md` Architecture (`lib/prompts/`, `lib/run.mjs`, "the drivers are interpreters"), and `CHANGELOG.md`.
- [x] 4.2 In `.claude/memory/`: delete `coupling/classifier-policy-lives-in-two-drivers.md`; rewrite `coupling/ship-projection-gates-record-batch-fields.md` and `coupling/ship-control-plane-signatures-pinned-in-two-tests.md` to name their new locations in `lib/run.mjs` and `test/spine/run.test.mjs`; keep `coupling/acp-driver-import-allowlist.md` (still true) and `coupling/ship-prompt-text-vs-workflows-test.md`; update `MEMORY.md`.

## 5. Validate

- [x] 5.1 Run `openspec validate emit-wave-steps-from-cli --strict`, `interlock validate emit-wave-steps-from-cli` and `interlock ledger emit-wave-steps-from-cli`; fix anything reported.
- [x] 5.2 Run `npm test` and `npm run validate` and confirm both are green, with the implementer fixtures unmodified.
