# Decisions — emit-wave-steps-from-cli

| id | question | class | resolution | evidence |
|----|----------|-------|------------|----------|
| D1 | New `run` subcommand family, or extend `wave-state next` to emit briefings? | agent_resolved | A `run` family that wraps the pure state machine; `wave-state` stays pure and replayable. | `lib/waves.mjs:36-53` (replayed transitions), `lib/waves.mjs:1808` (`nextStep` pure); design.md D1 |
| D2 | How does a briefing reach a Workflow-runtime worker when the script has no filesystem and result fields are capped? | agent_resolved | By reference: the CLI writes the briefing file with its hash on line one, the script spawns a fixed bootstrap, and a result that does not echo the hash fails closed with a named reason. | `openspec/specs/ship/cap-authority/spec.md:80` (every text field capped), `workflows/ship.js:767-778` (pings copy stdout), `openspec/specs/waves/spec.md` (missing packet fails closed); design.md D2 |
| D3 | Should the driver hold an action table, or should each step name its own continuation? | agent_resolved | Each step carries `then.argv`; the driver is a spawn-then-call loop with no action knowledge. | `.claude/memory/coupling/classifier-policy-lives-in-two-drivers.md` (loop knowledge in two files drifts); design.md D3 |
| D4 | Where do invocation flags live during a run? | agent_resolved | In a run manifest the CLI writes at `run start` and reads on every call; drivers pass flags once. | `bin/interlock-ship-acp:78-110` and `workflows/ship.js:38-99` (two parsers, host-specific delivery); design.md D4 |
| D5 | Fold merge and tick into `run record-batch`, or keep them as separate agent-mediated calls? | agent_resolved | Fold them: the CLI has the filesystem and git; three pings become one call. | `workflows/ship.js:906-969` (merge pings), `bin/interlock-ship-acp:893-895` (tick call); design.md D5 |
| D6 | Move the strict tail in this change, or leave it inline behind a seam? | agent_resolved | Leave it inline behind an explicit `host-tail` seam; the ACP driver keeps exiting `2`; the follow-on change deletes both. | `workflows/ship.js:2058-2356`, `bin/interlock-ship-acp:100-110`; design.md D6 |
| D7 | Which planner form do both hosts adopt: the agent runs the CLI, or the CLI runs after the agent? | agent_resolved | The ACP form: the planner writes `classified.json` and reports; `run classified` does the mechanical work. | `bin/interlock-ship-acp:597-651`; design.md D7 |
| D8 | Keep the stage-marker literal duplicated in the script? | agent_resolved | No: `lib/prompts/stage.mjs` renders it from `lib/ship-stage.mjs` and the drift test is retired. | `workflows/ship.js:704-737`, `lib/ship-stage.mjs:37-42`; design.md D8 |
| D9 | Where does the run-step bound live? | agent_resolved | `LIMITS.maxRunSteps`, enforced by the CLI; drivers keep a runaway backstop at the runtime's agent ceiling, named as not a cap. | `openspec/specs/ship/cap-authority/spec.md:9-31`; `bin/interlock-ship-acp:44`; design.md D9 |
| D10 | Who formats the run summary? | agent_resolved | `run close` returns the summary text; both drivers print it verbatim; host-only banners are handed in. | `openspec/specs/ship/completion-gate/spec.md:75` (the summary names every degradation); design.md D10 |
