## 1. Close the five review blockers

Independent files — one checkbox per production path. Inside 1.1 and 1.2, land the failing repro first, then the root-cause fix on that same path. Do not touch the 2026-09-14 warning list. Do not make outcomes or metrics fatal. Do not change `canonicalizePath`.

- [x] 1.1 In `lib/merge-lanes.mjs` export `foldMutationPaths(diffEntries)` per design D1 (`A`/`M`/`D` → `path`; `R` → `path` and distinct `oldPath`; `C` → `path` only). In `bin/interlock` `runMergeLanes`, set `changedByLane[label] = foldMutationPaths(diff)` instead of `diff.map(d => d.path)`. Leave `mergeDecision` and `applyLaneDiff` as path-set intersection / existing fold. In `test/spine/merge-lanes.test.mjs` (or a sibling under `test/spine/`): (a) unit-test `foldMutationPaths` for rename, copy, delete; (b) a git-worktree smoke that builds two lane worktrees from one base — lane A `git mv lib/a.mjs lib/b.mjs`, lane B edits `lib/a.mjs` — then `interlock merge-lanes --base <sha> --lanes <file> --json` exits non-zero, names canonical `lib/a.mjs`, names both lanes, and does not modify the shared tree; (c) the same fixture with a copy (`git cp` / copy then add) plus an edit of the source reports clean on that source. Verify: those tests fail on `main` before the mapping change and pass after; `mergeDecision` tests for `./` vs bare still pass.

- [x] 1.2 In `lib/run.mjs`, on an active `runId`, a failed run-trajectory write SHALL `haltStep('trajectory append failed: <site>: <reason>')` before task ticks and before commit (design D2): `logWaveMutation` `ok === false` at run start, record-batch, record-verify, and replan; `appendRunLogEvent` `written: false` for `agent-spawn`, `cli-exit`, `run-start`, `run-receipt`, `run-halt`, and `run-complete`. Leave `if (!outcome.written) ctx.warn(...)` unchanged. In `test/spine/run.test.mjs` inject `logWaveMutation: () => ({ step, ok: false })` on record-batch and assert the step is `halt`, the reason names the append failure, ticks were not invoked, and `ctx.warn` is not the only reaction; add coverage that an outcome-write failure still warns and does not halt. In `test/spine/cli.test.mjs` (or the existing wave-state tests) pin that `wave-state` and `verify judge` still exit non-zero on `!logOk`. Verify: the record-batch injection test fails on `main` (warn-and-continue) and passes after; outcome-failure still exits the same as today.

- [x] 1.3 In `test/hooks.test.mjs`, spawn `hooks/preflight.mjs` as a child process (design D3). Cover: (a) cwd where `interlock doctor` is clean enough to print the OK confirmation — exit 0, advisory context is brief; (b) cwd where doctor reports a failing check (empty tmp root is enough) — exit 0, output names the failing check and a fix string; (c) a copied hook tree whose `PLUGIN_ROOT` has no `bin/interlock` and `PATH` cannot resolve `interlock` — exit 0, output says the preflight could not run. Pin `.claude-plugin/plugin.json` SessionStart command still contains `hooks/preflight.mjs`. If the production `catch` cannot be reached without a seam, pin `process.exit(0)` and the ignored-error stderr token in the hook file instead of adding a test flag. Do not change fail-open behaviour. Verify: `node --test test/hooks.test.mjs` includes the new cases and they pass; deleting the SessionStart command from the manifest fails the pin.

- [x] 1.4 In `test/skills.test.mjs`, pin tokens (not sentences) per design D4: `skills/report/SKILL.md` contains `interlock report --json`, `Never recompute`, and `Licenses nothing`; `skills/spec/continuity.md` contains `interlock ready`, `--findings`, and the instruction not to compose a file containing a blocker count. Do not reword either file unless a token is actually absent. Verify: `node --test test/skills.test.mjs` passes; deleting `--findings` from `continuity.md` fails the new pin even if `interlock ready` remains; the existing leftover-Workflow pin is not treated as covering this contract.

## 2. Validate the change

- [x] 2.1 Run `openspec validate close-review-gate-blockers --strict`, `npm test`, and `npm run validate`. Verify: all three exit 0. If a test fails, fix the code from sections 1.x — do not weaken, skip, or delete the test.
