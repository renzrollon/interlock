# Decisions — surface-prompt-cache-cost

| id | question | class | resolution | evidence |
|----|----------|-------|------------|----------|
| D1 | Which environment produced the reported 5-minute main-conversation expiry, given this machine's transcripts show the 1-hour bucket? | agent_resolved | The Claude Code CLI runs the main conversation on the short lifetime; the desktop app runs it on the long one. The transcripts sampled during exploration were all app sessions, which is why they showed a surviving 32-minute idle. The CLI is the environment to address. | human decision 2026-09-07; explore brief §Pending Clarifications D1 |
| D2 | Does the change address the subagent lifetime alone, or both lifetimes? | agent_resolved | Both. D1 establishes the CLI's main conversation is also on the short default, so the advisory names `promptCacheTtl` and `subagentPromptCacheTtl` and says which governs wave agents. | design.md §Context; proposal.md §What Changes |
| D3 | Should the doctor prompt-cache check be able to `fail`, like `checkPermissions`, or advise only, like `checkNotify`? | agent_resolved | Advise only — `ok`/`skip`, never `fail`. An unset lifetime costs money, never correctness; a failing check would fire on every session that has not set it. | lib/doctor.mjs:732-750 (`checkNotify`); CLAUDE.md "Guards fail open, never closed" |
| D4 | May the doctor row state the lifetime a session is actually running under? | agent_resolved | No. Nothing exposes the effective lifetime to a hook, a script, or any command output. The row reports what the four settings scopes and the detected auth mode say, and asserts no effective value. | lib/doctor.mjs:61-73 (`settingsSources`), :872-880 (auth mode); specs/hooks/session-preflight/spec.md §"reports configuration, never an effective lifetime" |
| D5 | Do the cache-tier price multipliers edit `anthropic-list-2026-09` in place or mint a new `MODEL_PRICES` id? | agent_resolved | A new id. Repository doctrine is that revising a price table means a new id so historical figures stay attributable to the table that produced them. | lib/limits.mjs:250-285 |
| D6 | Is cache accounting implied by the usage capability a host already declares, or is it its own declaration? | agent_resolved | Its own declaration, with a spoken banner where absent. The Workflow runtime demonstrates the gap: it exposes a cumulative spend scalar and no cache decomposition. | lib/run.mjs:2230, :2263 (`TOKEN USAGE NOT REPORTED`); openspec/specs/run-host-adapters/spec.md §"Usage SHALL be recorded when the host reports it" |
| D7 | Are cache-creation figures recorded as one total or split by lifetime tier? | agent_resolved | Split by tier. The two tiers are priced differently, so a flattened total cannot be priced and the flattening is irreversible. | design.md §Decisions D7; specs/run-host-adapters/spec.md §"carried through unchanged or not at all" |
| D8 | May a host session identifier be written into `.claude/ship/runs`, a corpus some repositories deliberately commit? | agent_resolved | Yes, record it. It is an opaque host-generated identifier, no more identifying than the `runId` already recorded beside it, and without it the trajectory→transcript join stays timestamp-overlap — ambiguous in practice when two sessions overlap one run's window. No gitignore change accompanies it: the committed-corpus posture stays available. | human decision 2026-09-07; docs/11-the-indicators.md:75-83; design.md §Open Questions D8 |
| D9 | Does `interlock report` gain a cache indicator in this change? | agent_resolved | No. Whether a cache census falls on the permitted side of the report's non-recommendation boundary is a policy question; this change records the data and stops. Adding the indicator later needs no change to what is recorded. | docs/12-repository-review-policy.md; openspec/specs/report/non-gating/spec.md; proposal.md §"Explicitly out of scope" |
| D11 | How does a run acquire the host session identifier, given the Workflow script has no shell and the runner has no session of its own? | agent_resolved | `interlock run start` reads it from its own environment; neither driver carries or passes it, because a driver holding host-observed state is what the interpreter contract and the no-policy sweep both push into the CLI. On the Workflow host the CLI executes inside a ping subagent, so the implementation must verify the value observed is the parent session's and record absent rather than a subagent's identifier if it is not. | CLAUDE.md "the drivers are interpreters, not orchestrators"; test/workflows.test.mjs:3355-3410; specs/ship-run/spec.md §"SHALL record the host session identifier" |
| D10 | Does the change ship `experimental: { cacheTtl: 1h }` on `agents/worker.md`? | agent_resolved | No. Two official host pages contradict each other on whether the field survives plugin distribution, a one-hour write costs 2x base against 1.25x, and lane prefixes are already cold per worktree. The measurement this change adds is what would settle it. | explore brief §Options Considered option 5; design.md §Decisions D10 |
