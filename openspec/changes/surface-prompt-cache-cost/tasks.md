## 1. Advisory, measurement and the eval fix

- [x] 1.1 Add a `prompt-cache` advisory row to `interlock doctor` in `lib/doctor.mjs`, built as a sibling of `checkNotify` (`:732-750`): `ok`/`skip` only, never `fail`, never changing the overall verdict or exit code. Read the four scopes `settingsSources()` (`:61-73`) already opens for both lifetime settings, and the auth mode `:872-880` already distinguishes. Report configuration and the detected mode only — never an effective lifetime (D4) — and name the host version floor below which the settings are silently ignored, as a named constant in this module per the `INTERLOCK_*` reader convention. Cover in `test/spine/doctor.test.mjs`, including a case pinning that an unset lifetime does not produce a `fail` row.

- [x] 1.2 Fix the missing cache breakpoint in `evals/ship/agent/model.mjs:119-134`. **Land the failing assertion first**: a test that the loop's second request records a non-zero `cache_read_input_tokens`, which fails against today's code because the request marks no cacheable prefix and the provider therefore writes no entry. Then mark the stable prefix — the portion that does not change between turns of one loop — with `cache_control`. Keep the existing `emptyUsage()`/`addUsage()` tallies (`:46-65`) unchanged; they are already correct and were only ever reporting a structural zero. Where the prefix falls below the provider's minimum cacheable size, record the condition as unmeasured rather than as a miss. Do not touch any other behaviour of the eval loop — root cause only.

- [x] 1.3 Add cache-tier price multipliers to `lib/limits.mjs` under a **new** `MODEL_PRICES` id rather than editing `anthropic-list-2026-09` in place (D5), so figures stay attributable to the table that produced them. Carry a write multiplier per lifetime tier and a read multiplier alongside the existing input and output rates. Extend `formatLimits` so the new id is published like every other threshold, and pin the new id in the limits test.

- [x] 1.4 Record the host session identifier on `run-start` in `lib/run-log.mjs`, read from the environment by `interlock run start` in `lib/run.mjs` — never carried or passed by either driver (D11). Establish empirically whether the value in scope on the Workflow host is the parent session's or the ping subagent's, and record **absent** rather than a subagent identifier if it is the latter: a wrong identifier joins the trajectory to a transcript that is not the run's. Record absent on the runner, which has no session. Assert in `test/spine/run.test.mjs` that an absent identifier neither fabricates a substitute nor makes a run unreconstructable nor changes the exit code.

- [x] 1.5 In `lib/host/claude-cli.mjs:126-135`, widen `readClaudeEnvelope` to keep the usage envelope's cache-read figure and its cache-creation figures with the lifetime tiers separate — never summed, never substituting zero for an omitted field (D7). Treat an unparseable envelope as a host that did not report. Declare cache accounting as its own host capability in the registry, separate from the usage capability, so the run program branches on the declaration and never on an adapter's id or name (D6). Cover both in `test/spine/host.test.mjs`.

- [x] 1.6 **Depends on 1.5's parsed fields and capability declaration.** Record cache figures per wave and for the run, and speak their absence. In `lib/run-log.mjs` and `lib/receipt.mjs` carry the figures through the trajectory and the receipt under the same unknown-not-zero rule already governing output-token spend; in `lib/run.mjs` fold them in the host-observed merge (`:2228-2285`) and banner unavailability the way `TOKEN USAGE NOT REPORTED` (`:2263`) already does, so the summary states when a host cannot report rather than leaving an absence that reads as a measured zero. Cover in `test/spine/run.test.mjs`, including a host that reports figures for some spawns and omits them for others.

## 2. Documentation

- [x] 2.1 **Needs section 1 landed, to describe what shipped.** In `docs/04-when-it-stops.md`, separate the two things this repository currently both calls "cache": the Workflow replay cache already documented at `:111-117` (keyed on exact prompt text, what makes a resumed run skip completed agents) and the Anthropic prompt cache (keyed on the request prefix, expiring on idleness). Name both settings keys, which bucket each governs, and the version floor. In `docs/06-why-it-works.md §4`, extend the token-economy argument from prefix *size* (`§4.1`, `:150`) to prefix *lifetime*, and state plainly that Interlock cannot set either key. Update `CHANGELOG.md`.
