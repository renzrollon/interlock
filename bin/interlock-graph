#!/usr/bin/env node
// Entry point for the interlock-graph CLI.
// Claude Code adds a plugin's bin/ to the Bash tool's PATH while the plugin is
// enabled, so skills invoke this as a bare `interlock-graph` command with no
// path resolution ladder.
import './../lib/graph/cli.mjs';
