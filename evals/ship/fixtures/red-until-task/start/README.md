# parsekit

Parsers for the short human-written values a config file carries: durations and
comma-separated lists.

## Install

Nothing to install. Node >= 18, stdlib only.

## Test

```sh
npm test
```
